# 咕咕 后端环境变量示例
# ─────────────────────────────────────────────────────────────
# 用法：复制为 backend/.env（后端从 backend/ 目录启动时加载），按需填值。
#   cp .env.example backend/.env
#
# 配置两层：本文件(.env，基础值) + Admin 面板写的 config.override.json(优先级最高)。
# 敏感配置（AI key / OSS / 飞书凭据）建议启动后在 Admin 面板填，落到 override，不必写这里。
# 嵌套配置用双下划线 __，例如 DB__HOST 对应 settings.db.host。
# ─────────────────────────────────────────────────────────────

# === 安全（必填，生产务必换随机值）===
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# === 数据库 PostgreSQL（必填）===
DB__HOST=localhost
DB__PORT=5432
DB__NAME=gugu_web
DB__USER=pm
DB__PASSWORD=pm123

# === Redis（接 IM 频道时必填，用于消息队列）===
REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__PASSWORD=

# === AI Provider（建议在 Admin 面板配；这里填也可）===
# provider: qwen | openai | deepseek | minimax | anthropic
AI__PROVIDER=qwen
AI__API_KEY=
AI__BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
AI__MODEL=qwen-max

# === 存储（默认本地 uploads/；用 OSS 时改 backend=oss 并填下面）===
STORAGE__BACKEND=local
STORAGE__LOCAL_PATH=./uploads
# STORAGE__OSS_ACCESS_KEY_ID=
# STORAGE__OSS_ACCESS_KEY_SECRET=
# STORAGE__OSS_BUCKET=gugu-web
# STORAGE__OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# STORAGE__OSS_PREFIX=

# === 联网搜索 Tavily（可选，建议 Admin 面板配）===
# SEARCH__TAVILY_API_KEY=

# === 飞书频道（可选，建议在 Admin「频道」面板配；这里填是兜底）===
# FEISHU__APP_ID=cli_xxxx
# FEISHU__APP_SECRET=
# OAuth 扫码绑定回调（也可在 Admin 频道面板填），须公网可达 + 与飞书后台「重定向 URL」一致
# FEISHU__REDIRECT_URI=https://gugugu.site/api/v1/feishu/bind/callback
# 事件订阅用「请求地址」(Webhook) 模式才需要（长连接模式留空）：
#   回调地址填 https://你的域名/api/v1/feishu/event/<频道id>，与飞书后台 Encrypt Key/Token 一致
# FEISHU__ENCRYPT_KEY=
# FEISHU__VERIFICATION_TOKEN=
