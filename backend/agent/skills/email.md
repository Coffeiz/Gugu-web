---
name: 邮件
description_short: 用户要发送邮件、通知自己或联系客户时使用。
description_long: 使用咕咕标准模板发送 HTML+纯文本双格式邮件，并自动适配用户主题；默认发给当前用户注册邮箱，优先使用用户 SMTP，指定客户或其他地址时必须在发送前确认收件人。
category: communication
related_tools: send_email
emoji: ✉️
---

# 邮件发送 Skill

## 目标

通过 `send_email` 发送安全、可降级、跨客户端稳定的咕咕标准邮件。邮件服务负责 HTML 模板、主题配色、转义、清洗和 SMTP 发送；模型负责收件人、内容语义和模板选择。

## 必须遵守

1. `subject` 和 `body` 必填。`body` 始终写完整纯文本，作为所有客户端的降级版本。
2. 默认使用 `notification`，需要排版时传语义字段，不要自行拼接 HTML。
3. 工具返回 `status=sent` 后才能说“已发送”；这只表示 SMTP 接受，不代表最终送达。
4. 用户未指定收件人时省略 `to`，由服务端发送到当前用户注册邮箱；不要因为看不到邮箱地址而向用户索要。

## 模板选择

所有模板由同一个邮件服务渲染，并自动适配用户的主题和配色：

| 模板 | 使用场景 |
| --- | --- |
| `notification` | 普通通知、结果和消息 |
| `reminder` | 日程提醒、定时任务和待办 |
| `report` | 摘要、统计和进展报告 |
| `security` | 登录、安全告警和账号操作 |
| `test` | 仅 SMTP 连通性或样式测试 |

不要为了改变标题或正文而使用 `test`。

## 内容与排版

- 使用 `title`、`preheader`、`sections`（`heading`/`text`）和 `actions`（`label`/`url`）表达结构化内容。
- 需要间距、分隔线、强调块或按钮时，使用上述语义字段，由标准模板负责表格布局和兼容样式。
- 不要把 HTML 标签塞进 `body`；不要依赖自定义 CSS、外部字体、flex/grid、背景图片或复杂定位。
- 不要传 `html`，除非是受控的内部兼容调用且 HTML 来源可信；普通用户请求一律使用标准模板。

## 安全边界

- 禁止脚本、事件处理器、表单、iframe、SVG、外部 CSS、外部图片、追踪像素和未经用户要求的敏感信息。
- 操作链接只使用完整的 `https`、`http` 或 `mailto` 链接；不要使用 `javascript:`、数据脚本或未验证的重定向。
- 所有动态文案作为文本字段传入，不要把用户输入拼进 HTML 或 URL；服务端会负责转义和安全清洗。
- 不能通过 Skill 绕过服务端 sanitizer；如果排版依赖被清洗的标签或属性，应改用标准模板字段。

## 收件人与 SMTP

- 发送给用户本人：省略 `to`，使用注册邮箱。
- 用户明确指定其他邮箱：传 `to`，发送前确认收件人、主题和正文。
- 发送给客户：先获取当前用户拥有的客户 ID，再传 `client_id`；不要同时传 `to`。
- 客户没有邮箱时如实告知，不能猜测或改发其他地址。
- 优先使用用户设置中启用的个人 SMTP；未配置时回退到 Admin 默认 SMTP。

## 确认与定时任务

- 交互式发送前确认，不要把“准备发送”说成“已经发送”。
- 用户已授权的定时任务执行邮件时，不要再次请求确认。
- 邮件不可撤回；发送前重点核对外部收件人和敏感内容。

## 失败处理

- 失败时如实报告 SMTP 未配置、连接失败、认证失败或超时，不要声称邮件已发出。
- “已提交给 SMTP”只能表述为 SMTP 已接受，不要承诺收件箱一定收到。
