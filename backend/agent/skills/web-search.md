---
name: 联网搜索
description: "决定怎么获取外部信息：普通查找 / 深度研究 / 找图搜图。使用场景：用户要查资料、找官网文档事实新闻、要研究总结比较某件事、或要找一张图/配图/看看某样东西长什么样。"
emoji: 🔎
---

# 联网搜索技能

三个工具、各管一段，别死按顺序，按「任务性质」选：

| 工具 | 用途 | 成本 |
|---|---|---|
| `web_search` | 自建 SearXNG 通用网页搜索：找官网/文档/GitHub/某个事实/新闻标题/下载地址 | 免费、无配额 |
| `deep_research` | Tavily 深度研究：抓正文+清洗+给 answer，适合"读+总结/比较/研究/给引用" | 有每日次数配额（`SearchUsage`） |
| `image_search` | 自建 SearXNG images 分类：找图/配图/看某样东西长什么样 | 免费、无配额 |

## 怎么选

1. **普通查找**（找官网/文档/GitHub/某个事实/新闻/下载地址，**绝大多数联网都属此类**）→ `web_search`，返回标题+链接+摘要。
2. **需要读网页正文并总结/比较/研究/给引用**（"帮我研究…""总结这件事""比较 A 和 B""最近发生了什么"）→ 直接 `deep_research`，**这类别绕 web_search**，直接上更省事。
3. **要图**（找图/配图/看看某样东西长什么样）→ `image_search`。
4. **兜底**：`web_search` 搜索设施不可用、结果覆盖明显不足，或宽搜后仍无法满足任务时，转 `deep_research`。

## query 怎么写

`web_search` / `image_search` 面向传统搜索引擎，第一次调用优先把用户问题提炼成**实体 + 关键术语**的短关键词组合：

- 保留人名、组织名、项目名、产品名、版本号、年份/日期、关键技术术语。
- 不要把用户完整自然语言问题原样复制成一大句。
- 可以按需要混用中英文关键词；冷门技术项目英文名/仓库名应保留原文。
- **精确文件名、完整报错文本、论文标题、产品完整型号**本身就是高价值检索词，不要为了“短”而破坏它们。

例：用户问“最近 Anthropic 发布了什么新模型”时，`web_search.query` 优先类似 `Anthropic 新模型 2026`，而不是把整句问题原样传入。

## 看 `search_status` 决定下一步

SearXNG 搜索结果会带 `search_status.state`，不要把所有 `results=[]` 都理解成“网上没有”：

1. `state=ok`：基础设施正常且已有结果，直接使用；够答就停。
2. `state=empty`：搜索设施没有报告故障，但本次 0 结果。换同义词/中英文/更宽的一组短关键词再搜 **最多一次**。
3. `state=degraded`：部分搜索引擎不可用，覆盖下降。若已有足够结果就直接用；若 `results=[]`，最多做一次更宽查询，仍空再考虑 `deep_research`。
4. `state=unavailable`：当前配置的 SearXNG 引擎均不可用。**不要继续换关键词轰炸同一个 SearXNG**，直接改用 `deep_research`；这不代表互联网上没有相关内容。

`failed_engines` 里的 reason 是归一化诊断（如 `timeout` / `captcha` / `rate_limited` / `suspended` / `unavailable` / `unknown`），用于判断搜索设施状态，不要把它当成内容结论。

## 成本与预算

- 只有 `deep_research`（Tavily）计费、有每日次数——能用免费的 `web_search`/`image_search` 搞定就别动它；这两个走自建搜索，**免费，放开用**，只是别为同一个已知答案反复搜。
- **先做一次宽搜**；只有「结果不够/缺关键事实/用户要穷尽/要某份具体文档」才再搜一轮，别为措辞更顺、想多凑点料而反复搜同一件事——够答就停。
- `empty` 才优先考虑改关键词；`unavailable` 是观测手段失效，不是内容不存在，直接走兜底。

## image_search 搜到图后怎么发

`image_search` 只返回候选列表（标题+来源页+图片直链 `img_src`+缩略图），**不会自动发送**。

用户明确要看图/要一张图时——找图本身就是要看/要发，没有额外的"保存"步骤——搜到后**同一轮直接接着调 `send_file(url=选中候选的 img_src, title=简短描述)`** 把图发进对话/IM，不用再问一句"要不要发"。

多个候选且用户没指定要哪张时，挑最匹配关键词的第一条发；用户明确想多看几张再多发几张（IM 端一次发太多张会很吵，建议 1~3 张为宜）。

注意：这条和 `文件操作` 技能里"`send_file` 只在用户明说「发给我」时才调（保存≠发送）"针对的是**文件库文件**——库文件有"生成/保存"和"发送"两个独立动作，不能默认发送；网络图片没有保存这一步，两者不冲突。
