# 开发/测试专用依赖（生产不装）：pip install -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20        # 测试用内存 SQLite（conftest.py），不碰真实 Postgres
fakeredis[lua]>=2.30   # 测试用进程内 Redis（conftest.py），不碰共享 Redis；lua 供 lock 的 release/extend
watchfiles>=0.21       # 开发环境监听 Worker 代码并自动重启
pytest-cov==7.1.0      # 可选质量报告：pytest 覆盖率 JSON
lizard==1.23.0         # 可选质量报告：Python/TypeScript 圈复杂度
mutmut==3.8.0          # 周期性手动变异测试；不得接入自动 CI
pytest-xdist>=3.5      # 并行分片：内存 sqlite/伪造依赖逐 worker 独立，可直接 -n auto
