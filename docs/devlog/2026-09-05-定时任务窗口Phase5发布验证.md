# 定时任务窗口 Phase 5 发布验证

日期：2026-09-05

## 结果

定时任务开始/结束时间与精确 Interval 窗口已完成 Phase 5 验收。实现同时清理了运行时对旧 `@once`、Interval cron 推断和缺失字段猜测的兼容分支；旧数据转换只保留在一次性 Alembic 迁移中，迁移回归测试继续保留，用于保护升级路径。

## 本地检查

- 后端完整 pytest：`1979 passed, 3 warnings`。
- 前端 Vitest：`69 files passed, 432 tests passed`。
- 前端 strict typecheck、production build、ownership/confirm 检查和 Python compileall 均通过。
- `git diff --check` 通过；未删除迁移回归测试或仍承担旧数据保护职责的代码。

## Devserver 检查

- Alembic 当前 revision 为 `llm23_phase3_model_policy`，已包含 `20260905000004_add_scheduled_task_schedule_window`。
- 迁移后任务统计：总数 16，cron 10，interval 0，once 6，非法值 0；其中日历活动提醒 5 条，运行时旧格式 0 条。
- 16 条启用任务全部成功构造 APScheduler trigger，全部有下一次触发时间；下一次触发范围为 `2026-09-05 21:30` 至 `2026-10-29 09:00`（Asia/Shanghai）。
- 用隔离合成任务等待真实 worker reconcile：结束时间到达后任务自动 `enabled=false`，任务记录仍保留。
- 用隔离合成用户走通咕咕工具创建、更新、查询；随后调用正式执行与 Web 投递链路，通知成功持久化。合成用户、任务和通知已清理。
- 验收过程中发现 backend 处于一次旧的 `deactivating` 重启收尾状态，使用既有白名单 systemd restart 恢复；复查 `gugu-sandboxd`、`gugu-backend`、`gugu-worker`、`gugu-gateway` 均为 active/running。

## 镜像与安全扫描

- 使用无缓存生产镜像构建，镜像内执行 `node bin/gugu-rag-ts-worker.mjs --version` 成功，返回 `gugu-rag-ts-worker 0.2.0`。
- Trivy 使用 release workflow 相同的 pip vendored BOM 排除项扫描实际镜像目标，HIGH/CRITICAL 且 `--ignore-unfixed` 结果为 0，扫描通过。
- 临时验收镜像与扫描报告已删除，已有 `trivy-cache` 保留；未触碰独立 npm/py 前后端容器。

## 备份与迁移审计说明

本轮接手时迁移已经完成，默认备份目录中没有可确认的迁移前备份，因此不能将停 worker、迁移前备份和迁移前统计写成已亲自完成。已补做迁移后完整备份并通过 `pg_restore --list` 结构校验：

```text
backend/.deploy-backups/gugu-backup-20260905-183024.tar.gz
大小：642M
数据库服务端：PostgreSQL 18.6
```

此外修复了 `backend/backup.sh` 在 `PGPASSFILE` 不存在时无法通过 `chmod` 的问题，并增加可选 `PG_DUMP_IMAGE`，使 PostgreSQL 客户端版本可与服务端对齐；默认宿主机工具路径保持不变。
