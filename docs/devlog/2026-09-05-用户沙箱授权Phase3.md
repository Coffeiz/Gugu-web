# 用户沙箱授权 Phase 3

日期：2026-09-05

## 完成内容

- 文件库的创建、保存、编辑、改名、移动、复制、建/删文件夹，以及回收站恢复和永久删除，统一读取当前 Session 或定时任务主体的 `filesystem policy`。
- `web_download` 在发起公网请求前检查同一套写权限，未授权时不会先下载再在持久化阶段失败。
- 未绑定 workspace 且未获得完整授权时，personal/project 文件位置保持只读；绑定 workspace 时只允许写入 workspace 根及其子树；完整用户沙箱授权解除该位置限制。
- 新增 `run_script` 工具，限定为用户明确指定的相对脚本路径和 `python3`、`node`、`bash` 三类解释器。
- 脚本入口拒绝绝对路径、路径穿越、Windows 分隔符、软链接、硬链接和 Shell 控制字符，并继续复用 Shell 的 sandbox、超时、输出、网络隔离、授权复核和进程清理。

## 验证

- `PYTHONPATH=. .venv/bin/pytest -q tests/test_phase3_filesystem_policy.py tests/test_web_download.py tests/test_shell_sandbox.py tests/test_docker_runtime.py tests/test_tool_schema_security_contract.py tests/test_tool_schema_validation.py`
- 结果：145 passed，2 warnings。
- 受影响回归：Shell、PTY、定时任务、命令、文件、回收站共 146 passed，2 warnings。
- `PYTHONPATH=. .venv/bin/python -m compileall -q agent app tests` 通过。
- `git diff --check` 通过。
- `scripts/audit_tool_schemas.py` 仍报告既有 note/web 描述长度提示；本阶段没有新增该类问题。
