# RAG Phase 0/1 实施进度（2026-09-09）

## 已实现

- 自动召回开始时创建 LoopScope span，成功、超时、容量上限、取消和内部错误在等待边界结束；迟到后台结果不能创建第二条成功记录或改写注入状态。
- 来源进度记录索引准备、sidecar 查询、数量统计与完成/失败状态；修正 sidecar 时间包含 DB 统计的问题，补齐 source_diagnostics 传递。
- snapshot 缓存锁内复查命中，区分 transient-only 条目与已经加载持久化来源的条目。
- TS `batch_search` 一次分词和 BM25 计分，按来源/scope 独立截断；Python 保留最终权限复核和 conversation 水位。
- Memory 并入一次总查询：worker 新增 `replace_transient` 瞬态语料槽（只驻内存不落盘），`batch_search` 的 search 项可声明 `corpus: "transient"`；两个语料槽各自计算 BM25 统计边界，不合并文档。瞬态槽按快照指纹驻留，指纹未变时零额外 IPC；Python 记录进程代数，worker 重启后自动重传。
- `BatchUnifiedRetriever`：一次索引准备 + 一次 `batch_search` 同时取回持久化与 Memory 候选；`search_knowledge` 按 `search.rag_query_mode` 选择 legacy / `batch` / `batch_shadow`。
- `ShadowUnifiedRetriever`：交付 legacy 结果，影子批量结果只记录候选差异（`shadow_equal`、`shadow_first_diff_index`、`shadow_batch_count`、`shadow_total_ms`）进 source_diagnostics；影子失败只记 `shadow_error`，永不影响交付。
- 构建类 op（`replace`/`patch`/`build_and_index`/`replace_transient`）使用独立的 30s 构建超时；500ms 搜索超时只约束搜索类请求。此前全量索引装载超过 500ms 会被误判为 worker 不可用（batch 模式冷启动必现）。
- worker JSONL 流上限放宽到 32MB：批量查询响应聚合多来源候选原文，单行超过 asyncio 默认 64KB 时 readline 抛 "chunk is longer than limit"，现在会以 `TsSidecarUnavailable` 显式关闭重连而不是留下半行错位。
- 配置默认 `legacy`，未变更运行配置，未自动切换用户查询。

## 测试

- 协议层（`test_rag_batch_protocol.py`）：混合语料时 Memory 候选分数与「只装 Memory 语料的独立索引」逐项一致（9 位小数）；空瞬态指纹被 worker 拒绝；worker 重启后 Python 按新进程代数自动重传瞬态语料、持久化索引从磁盘恢复。
- 检索器层（`test_rag_batch_retriever.py`）：Memory 与持久化来源共用一次 prepare + 一次批量 IPC（`prepare → transient → batch`）；memory-only 显式查询退回 legacy 路径且不触碰批量索引；file/canvas/note/project/conversation 的 scope 收口矩阵；shadow 交付 legacy 结果并正确记录等价/差异元数据。
- 超时回归（`test_rag_ts_sidecar.py`）：构建类 op 走构建超时、搜索类 op 走配置超时。
- TS worker 协议测试：`replace_transient` 与持久化索引共存且不污染持久化 revision；混合语料 `batch_search` 一次取回双方命中并拒绝空/错误瞬态指纹。
- 本地后端全量 2305 通过；TS 全量 39 通过，typecheck 通过。

## devserver 全链路验收（真实账号，4063 chunk / 7 来源）

`scripts/bench_rag_recall_modes.py`（新增）驱动 `search_knowledge` 主链（准备、排队、检索、排序、注入组装），不含 LLM 意图路由与重排；每模式独立进程保证冷口径一致；30 轮热 + 6 并发 × 4 轮突发：

| 指标 | legacy | batch |
|---|---:|---:|
| 冷启动 | 2115ms | 1384ms |
| 热 P50 / P95 | 157ms / 236ms | **38ms / 59ms** |
| 并发 P50 / P95 | 1214ms / 1636ms | **169ms / 369ms** |

batch 模式全部来源共享一次索引准备（P50 22ms）和一次 sidecar 查询（P50 6ms，排队 0ms）；legacy 每来源独立排队 32–89ms 且冷启动并行重建互相踩踏（canvas 单项构建 598ms）。热路径端到端 P95 59ms，远低于 500ms 验收线；3 秒总预算下的真实超时大头确认为 legacy 的每来源串行排队与索引重建叠加，而不是单次 IPC。

等价验证（`batch_shadow` 同帧对比，3 轮 × 6 查询）：

- 持久化来源 canvas/conversation/file/knowledge/note/project：候选 ID 与分数（6 位小数）18/18 完全一致，无权限扩大。
- Memory 来源 shadow 不一致（18/18），根因在 legacy 侧：legacy 的 Memory 走 `replace` 覆盖 worker 唯一持久槽（replace-churn），shadow 流程中持久槽被其他来源/全量语料占据时，Memory 候选是对错误语料计算的 BM25（分数随语料漂移）。batch 侧 Memory 固定查独立瞬态槽（455 文档），协议测试证明与隔离索引一致，属确定性正确侧。分进程跑 legacy/batch 对比时，legacy 进程最终注入几乎只剩 memory 结果、batch 进程各来源均衡，同样是该缺陷的表现。
- 诊断口径注意：batch 的 `document_counts.memory` 是持久槽 + 瞬态槽合并计数（870 个已索引 memory chunk + 455 瞬态文档 = 1325），Memory spec 实际只查瞬态语料。
- legacy 并发 replace 与批量复用检查交错时可触发 `TsSidecarUnavailable`（批量查询索引版本不一致）；shadow 模式正确隔离为 `shadow_error`，交付不受影响。生产 batch 模式无 legacy 腿，并发召回 24 样本 0 错误。

## 结论与后续

- Phase 0/1 验收通过，PRD 已勾选；`rag_query_mode` 默认保持 `legacy`。切换默认前建议生产开启 `batch_shadow` 观察 shadow_equal 统计，由产品决策。
- devserver 上的 worker 制品需用 esbuild 重建（`bin/gugu-rag-ts-worker.mjs` 已随本次提交更新）；只同步 Python/TS 源码不重建制品时，batch 模式会报「未知操作」。
- 完整 `pnpm build` 链路在 devserver 仍受 Makefile PATH 无 pnpm 限制，本次用已安装 esbuild 按 RAG 构建参数单独生成制品并通过 `--version` 校验语义（op 级协议测试兜底）。

# Phase 2/3 追加（2026-09-09）

## Phase 2：TS 统一候选排序

- 契约审计确认 TS `rank_candidates`（confidence-v1）已是唯一评分实现，Python 正常路径只做映射与回填；清理 contracts 里从未传输的 `rank` 字段。
- `TsSidecarClient.rank_candidates_with_cache` 增加 scoring_version 守卫：worker 返回版本 ≠ `confidence-v1` 时抛 `TsSidecarUnavailable`，评分器代际漂移响亮失败而不是静默换口径。
- 冻结测试钉住 tie-break 真实契约：SOURCE_PRIORITY 只影响 confidence 入选顺序，最终输出按 fused 降序 + id 升序重排（首个测试按臆想顺序断言失败后修正为真实行为）。
- `batch_shadow` 增加 rank 影子：batch/legacy 两侧候选分别送 TS 评分器，记录 `rank_scoring_version`、`rank_equal`、`rank_first_diff_index`。
- 排查记录：诊断模式冷启动偶发 rank 影子全空的根因是 legacy 单一 owner 级 revision 设计（各来源共享 max(indexed_at)，后建来源跳过重建、瞬态 replace 劫持持久槽），属 legacy 已知结构缺陷，batch 为确定性正确侧；shadow_error 隔离不影响交付。

## Phase 3：TS 接管 hybrid 融合

- worker 新增 `hybrid_fuse` op，与 Python `hybrid_results` 逐位对齐：RRF 浮点顺序 `weight * ((K+1)/(K+rank))`、cosine 含 sqrt、维度不匹配/零向量记 0.0 但保留向量名次、vector_map 非空而命中均无可用向量时按纯词法 RRF 重打分（不是透传原始分——首版漏了这条，靠等价测试逼出）。
- `TsSidecarClient.hybrid_fuse`：退化输入（无查询向量/空缓存/命中均无可用向量）本地走 `hybrid_results`，语义逐位一致且避免空载 IPC；正常输入只传候选子集向量（整份向量表约几十 MB，不进 IPC）。
- `_memory_finalize` 增加 `fuse` 注入点：batch 主链传 TS 融合闭包，legacy 保持 Python 融合作为回滚；worker 融合失败显式记 `ts_hybrid_error` 后回滚 Python 实现，不静默兜底。
- 向量仍由 Python 生成与加载（provider/密钥/配置边界不变）。

## 测试

- TS：hybrid_fuse 手工 RRF 冻结值（1e-12）+ 透传契约测试；全量 43 通过，typecheck 通过。
- Python：真 worker 等价测试（全量/部分缺失/脏向量/命中均无向量/空缓存/无查询向量六种情形，9 位小数对齐候选身份、排序与 fallback）；后端全量 2308 通过。

# Phase 4 追加（2026-09-09）

## 索引生命周期收口与可观测

- worker 恢复磁盘索引时区分三种结局：无索引文件（首次冷启，非损坏）、version_mismatch、corrupt；后两者经 ping 的 `restore_error` 显式上报，Python 侧存为 `restore_error` 属性并透传进查询诊断 `index_restore_error`。损坏索引不再无声跳过；全量重建成功后 worker 与 Python 双侧清空。
- 新增测试：损坏报告与重建恢复（TS + Python 双侧，含 owner_hash 子目录真实布局）；并发 patch/replace/search 管线化请求下 revision 单调推进、每个检索响应与其声明 revision 严格一致（首版断言用错删除键 `d0:1:0`——worker patch 的 deletes 是裸文档 id，测试失败逼出正确契约）。

## Phase 4 遗留的决策

- 「TS 统一处理 source adapter 与分块」的写路径移交未实施：worker 统一 builder 已就绪但其文本方言与 Python 投影不一致（TS file 适配器拼 relative_path，Python 拼「文件：/类型：/空间：/阶段：」头部），切换会改写已入库 chunk 文本与版本；且 KnowledgeIndexEntry 表是跨进程持久 chunk 事实源，分块必须发生在写库前。移交需三步（方言对齐 → 双实现投影等价 → shadow 投影），列为后续独立阶段。Phase 5 查询主链不受影响：worker 语料本来就是 Python 投影后经 replace/patch 提交的文档。

# Phase 5 追加（2026-09-09）

## 统一查询主链收敛

- worker 新增 `unified_query` op：一次 IPC 完成 BM25 召回（逐 spec scope/来源过滤）、来源内聚合去重
  （keep-first + `(-score, id)` 截断）、conversation 水位（只作用于 `kind=message` 文档）、Memory 混合
  融合（向量随 `replace_transient` 驻留，指纹 = 语料指纹 + 模型版本戳）与 confidence 排序全量契约。
- TS golden 测试钉死等价：与 batch_search + hybrid_fuse + rank_candidates 三段参考管线逐位一致
  （选中序列与 confidence 1e-12）。调试过程三个坑都有价值：
  1. 参考管线 memory 候选误从持久语料 `find` 文档拿到 `undefined` 展开成无正文文档，confidence<0.35
     全被拒——两边候选池文本不同，等价断言自然崩；修正为瞬态文档单列共用。
  2. 参考管线 `hybrid_fuse` 的 hits 传了 `id` 没传 `chunk_id`，向量查不到静默退化纯词法，
     `fusedScores.get()` 全 undefined → memory raw=0；正是 Phase 3 踩过的「两边都退化等价断言虚过」
     坑的变体，这次靠防退化断言（`fused.fusion === "hybrid-rrf"` 硬断言）当场逼出。
  3. worker 键（无版本 `type:parent:chunk_index`）与候选 id、chunk_id（含版本）三套键并存，
     对比一律走 candidate-id → document-key 映射，不做字符串前缀猜测。
- Python `UnifiedQueryRetriever`：业务装载 + 权限事实 + 向量生成 + 一次 `unified_query`；
  `_resolve_rank_rows` 把 worker 选中行回连 Python 文档成 `(candidate, text, row)` 三元组，
  `RetrievalBatch.rank_rows/rank_stats` 随批上送；service 检测预排序批次（len==1）跳过二次排序。
- 如实记录偏差：权限复核在排序后执行（worker 只见已授权 spec 召回结果，Python 复核为第二道防线，
  越权行剔除计数、不回补预算）；`fallback` 标签按 Python 事实判定（embedding_disabled / worker 回报
  / None）；统一主链 `strategy` 按融合事实（metadata `fusion=hybrid-rrf` → hybrid）而非沿用老路径
  恒 bm25 的写法。
- 修复 shadow 检测真 bug：`UnifiedShadowRetriever` 成功影子批 source_type 是 `unified`（只有失败才落
  `unified-shadow`），service 原检测条件永远匹配不上，`unified_equal` 对比从未写入过。
- 灰度：`rag_query_mode` 五档（legacy | batch | batch_shadow | unified_shadow | unified），默认 legacy，
  配置静态读取无运行时切换；影子永不交付，对比写诊断。

## 测试

- TS：worker 协议 20 用例全绿（含 golden 等价 + 防退化断言）；typecheck 通过。
- Python：新增 `test_rag_unified_query.py` 7 项（单 IPC 调用事实与指纹耦合模型戳、watermark 下传、
  fallback 三态、service 预排序装配、越权剔除不回补、影子隔离、影子参数透传）；后端全量 2318 通过。

# devserver 真链路验证追加（2026-09-09）

## Phase 4/5 live 验证（devserver 5173 栈）

- 重建 esbuild bundle 到 ``backend/bin/gugu-rag-ts-worker.mjs``（注意 ``npm run build`` 的 outfile
  相对路径落在未跟踪的 ``ts/bin``，tracked 制品在 ``backend/bin``，用三级上跳重建）。
- unified 主链一次 IPC 真数据交付正常（engine=typescript、unified_query=True、candidates=14）；
  warm retrieve_ms P50 从 legacy 的 554ms 降到 28ms（探针级粗测，同查询 8 轮，非严格基准）。
- unified_shadow 影子对比诊断在真实数据上写入（``unified_equal`` / ``unified_first_diff_index`` /
  ``unified_selected_count``），影子永不交付。
- legacy 与 unified 交付序列存在真实差异（如 legacy 5 行 vs 影子 4 行，first_diff_index=1）：
  两模式候选集语义不同（legacy 逐来源独立 limit、Python 侧水位过滤 vs worker 侧一次聚合），属于
  灰度评估项而非缺陷；切换 unified 前需以影子诊断观察足够样本。
- 灰度阶梯验证：配置默认 legacy 未动；legacy → unified_shadow → unified 内存切换全链可用，
  影子失败/主链失败均显式异常不静默。

## 修复：worker 重启后瞬态语料误跳重传（真链路逼出）

- 复现：kill worker 后首个 unified 查询报「TS 统一查询瞬态语料版本不一致」，cold_matches_warm
  失败。根因：``replace_transient`` 的幂等短路判断在 ``_ensure_process`` **之前**执行——worker
  死亡后 Python 侧残留旧 revision 与旧代数，短路误判「已加载」，新进程瞬态槽为空。
- 修复：短路判断前检查进程健康度，进程不健康时先失效 ``_transient_revision`` 并按新代数判定，
  保证必然重传；进程健康时幂等短路保持不变。新增回归测试
  ``test_replace_transient_reuploads_after_worker_death``。修复后 devserver 实测
  ``cold_matches_warm=True``（4 条同序），后续查询恢复零 IPC 短路。
- 首轮验证还出现过一次持久槽「索引版本不一致」：发生在 bundle 热替换 + 服务重启 + 磁盘索引由旧
  bundle 写入的组合瞬态窗口，legacy 冷启动重建兜住后未再复现；磁盘恢复的 version_mismatch 重建
  路径（Phase 4）覆盖该场景。

# 旧 Python RAG 代码清点（2026-09-09，Phase 5 收尾）

范围：`agent/rag/**` 全量 + `agent/memory` 邻接模块扫描（孤儿函数扫描 + 逐模块引用核查）。

## 已删除（真死代码）

- `agent/rag/models.py::stable_version` 及其孤儿 `json` import：旧 Python 版本号生成残留，
  全仓库（agent/app/tests/scripts）零引用，已被 `chunk_id`/`text_version` 方案替代。

## 确认不存在

- Python 侧独立 BM25/词法索引实现：grep 全目录零命中，tokenizer 与 BM25 从未在 Python 侧
  （与 PRD 责任边界一致），无可清理的重复词法实现。
- 除 `stable_version` 外无孤儿函数；扫描初报的 memory 侧「孤儿」均为调度注册/测试引用误报。

## 保留的回退路径（受 PRD §9 灰度门约束，灰度完成后才允许清理）

1. `hybrid.py` + `fusion.py`：Python 融合的冻结契约参照；同时是 `TsSidecarClient.hybrid_fuse`
   退化输入的本地执行路径和 batch 主链 worker 失败回滚（`ts_hybrid_error`）的实现。
2. `UnifiedRetriever` 旧多请求路径：`rag_query_mode=legacy`（当前生产默认）的交付路径，
   也是 `unified_shadow` 的 legacy 侧。
3. `MemoryRetriever` / `ProjectRetriever`（service.py）与 `ts_sidecar` 单查询 `search`：
   legacy 交付链在用。
4. `persistent_store.py` / `storage.py` / `index_builder.py` / `pipeline.py` / `chunking.py`：
   索引写路径（Phase 4 遗留的 TS 写路径接管完成前，Python 投影 + KnowledgeIndexEntry 表是
   跨进程 chunk 事实源）、legacy DB 读路径与 `/api/v1/search` 在用。
5. `rank_candidates_with_cache`：TS ranker 的 Python 调度壳（含查询缓存），batch/legacy/shadow
   排序仍经它。

## 后续清理触发条件

- 灰度全量切 `unified` 且观察期通过 → 删 2/3（旧多请求路径与旧来源 retriever 交付链）。
- Phase 4 遗留写路径移交完成 → 重新评估 4（写路径 Python 投影与分块）。
- `hybrid.py`/`fusion.py` 若定位为纯契约对照，可随移交移入测试域（届时决定）。

# Phase 4 写路径移交第一步：record 管线 + TS 适配器方言对齐（2026-09-09）

目标：把「Python 投影方言」与「TS worker 适配器」收敛到同一份 source record 协议，建立双实现
投影等价基线（PRD-RAG-7 Phase 4 三步走的第①②步：方言对齐 + 等价测试；shadow 投影与生产切换
留给第③步）。

## Python 侧：build_source_documents 重构为 record 纯函数管线

- 新增 `*_record(row, ...)` 纯函数（file/note/canvas/calendar/scheduled_task/conversation_summary/
  conversation_message）：从 ORM 行提取统一 source record（`source_type`/`id`/`title`/`version_parts`/
  `updated_at` + 各来源业务字段），不再直接拼文本。
- `record_text`（逐来源头部方言）/ `record_metadata` / `record_title` / `record_documents`（strip→
  split_text(1400)→text_version→IndexDocument）构成投影纯函数层；`build_source_documents` 六个
  分支改为「查库 → *_record → record_documents」，行为经 195 项 RAG 相关测试确认无回归。
- `version_parts` 统一序列化为字符串（datetime→isoformat）。**一次性代价**：与旧 `str(datetime)`
  （空格分隔）不同，部署后各来源 document_version 变化会触发一轮全量重传（自限、无功能影响）。

## TS 侧：适配器消费统一 record 协议 + wire 口径对齐

- `files.ts` 字段 `display_name`→`title`、`canvas.ts` `item_id`→`id`、`conversations.ts` 改为
  `id`/`session_id`/`kind` 协议（summary=`<会话id>:summary`、message=`<消息id>`，不再要求
  `message_id` 字段）；`note.ts`/`calendar.ts`/`scheduled-tasks.ts` 本轮新增，字段协议天然一致。
- wire 口径对齐 `_worker_document_key`：**id = `source_type:document_id:chunkIndex`（document_id
  本身已带一次 source_type 前缀，双重前缀是现网事实）**，`parent_id` 保持单前缀；text = title↵
  summary↵content 三段拼接；`textVersion` sha256 与 Python `text_version` 逐位一致（冻结值测试）。
- `validScope` 放宽到生产现实：Python 生产 owner scope 就是 `scope_type="owner"`、`scope_id=""`，
  旧实现要求 scope_id 非空把 owner 记录全滤掉（等价测试逼出）；改为 owner 只查 scope_type、
  group 必须携带 scope_id。

## 双实现投影等价测试（等价基线）

- `backend/tests/test_rag_source_projection_equivalence.py`（5 项）：同一份 record 分别走 Python
  `record_documents`→`_wire_document` 与真实 worker 进程（`--experimental-strip-types` 跑 TS 源码）
  的 `adapt` op，wire 域逐字典全等。覆盖 file（全字段/最小/3000 字长文分块）、note（匿名标题回落）、
  canvas（全字段/裸节点）、calendar+scheduled_task、conversation（摘要+消息、群 scope）。
- `ts/workers/rag/test/source-adapter-equivalence.test.ts`：TS 方言冻结（版本 sha256 冻结值、
  「文件/类型/空间/阶段」头部、三段 text 拼接、消息元数据）。

## 验证与制品

- 后端全量 2324 通过；TS worker 37 通过；typecheck 通过；`backend/bin/gugu-rag-ts-worker.mjs`
  重建（45.3kb）并冒烟 ping + adapt（wire id/parent_id/text 口径正确）。
- 待办：第②步 shadow 投影（TS 投影结果与 Python 写库比对）与第③步生产切换；切换前
  KnowledgeIndexEntry 表仍是跨进程 chunk 事实源。

# Phase 4 写路径移交第二步：shadow 投影比对（2026-09-09）

目标：同一批 source record 分别经 TS worker ``adapt`` 投影与 Python 写库构建，chunk 级
（id/text/document_version/metadata）影子比对并记录诊断；比对基线通过后规划第③步生产切换。

## 实现

- ``agent/rag/write_shadow.py``：``shadow_compare_build`` 把 TS ``adapt_records`` 输出与写库产物
  ``_wire_document`` 在 wire 域逐字段比对；诊断只记结构信息（chunk 数、首个差异 chunk 标识、
  差异字段名、两侧 content 长度），**永不抛出、绝不改写写路径**（影子失败=观测缺失，不是索引错误）。
- 挂点：``pipeline.rebuild_source_index``（增量事件，写库与向量同步之后）+
  ``rebuild_knowledge_index``（全量重建，commit 之前）；六来源 record 管线来源生效，
  memory/knowledge/project 无 record 管线自动跳过。
- ``index_builder`` 抽出 ``build_source_records``/``documents_from_records``：查库与投影分离，
  增量路径不再双查询；``ts_sidecar`` 新增公开 ``adapt_records``（只投影不触碰索引）。
- 开关 ``search.rag_write_shadow``（默认关，env ``search__rag_write_shadow``）。
- 新增 ``tests/test_rag_write_shadow.py`` 6 项：真实 worker 一致、篡改写库产物抓漂移、
  关闭跳过、无管线跳过、worker 故障隔离（error 诊断不抛出）、重建集成。

## devserver 真实数据抓到一处真实方言缺陷（影子比对的价值实证）

- 首轮比对六来源中 4 来源一致，file（first_diff_index=51，chunk file:1904:0，左 1398/右 1384）
  与 conversation（index=178）不一致，diff_fields=content/text。
- 根因：JS ``String.length``/``slice`` 按 **UTF-16 码元** 计数，Python ``len``/切片按 **Unicode
  码点**——正文含非 BMP 字符（emoji）时，1400 边界的切分窗口错位；摘要 ``text[:240]`` 同理。
- 修复：TS ``chunkText`` 改为码点数组（``Array.from``）测量与切片（含 overlap tail），摘要截断
  同口径；``test/source-adapter-equivalence.test.ts`` 与 Python 等价测试各补非 BMP 用例
  （1360 码点含 emoji 不误切、1420 码点切窗逐位对齐）锁定。
- 教训：等价测试的合成用例全 ASCII/常用汉字，UTF-16 与码点口径无差异；真实用户数据里的 emoji
  才暴露。分块这类「长度敏感」的跨语言对齐必须用非 BMP 用例冻结。

## 验证与常开

- 修复后 devserver 两用户六来源影子比对全部一致（382+62+44+19+7+2859 与 3+0+0+0+0+8 个 chunk
  逐字段全等，含 0 chunk 空来源边界），单来源 IPC 比对耗时 1–483ms。
- ``search__rag_write_shadow=true`` 已写入 gugu-backend/gugu-worker systemd drop-in 并重启生效，
  持续积累真实事件影子基线（只记诊断）。
- 回归：后端全量 2330 通过、TS worker 38 通过、制品重建并冒烟（码点切窗验证）。
- 待办：第③步生产切换按 PRD 规划执行（影子基线观察期 + ``rag_write_mode`` 三态开关 +
  ``_from_wire_document`` 落库通道）。

# Phase 4 写路径移交第三步：rag_write_mode 三态开关 + TS 投影落库通道（2026-09-09，默认 python 未启用）

前置基线检查：devserver gugu.log 在影子开关常开后尚无自然索引事件（重启后无业务写入），
无 mismatch 记录；现行基线为确定性探针（两用户六来源 3384 chunk 全等）。观察期继续由
常开开关积累，mismatch 会同时进 gugu.log（WARNING）与 SystemLog 表（WARNING+）。

## 实现

- 配置新增 ``search.rag_write_mode: Literal["python", "ts"]``（默认 python，env
  ``search__rag_write_mode``）。python 模式行为与移交前逐位一致（全部既有测试未改动通过）。
- ``ts`` 模式：``records_to_write_documents`` 把 TS ``adapt_records`` 返回的 wire 文档经
  ``wire_document_to_persistent`` 回转 IndexDocument 落 KnowledgeIndexEntry；worker 索引沿用
  既有 replace/patch 通道（持久行身份 document_id=parent_id 单前缀口径，模式切换不改身份，
  ``_worker_document_key`` 由 parent_id+chunk_index 可完整重建）。
- **偏离说明**：未直接用 ``_from_wire_document``——它面向查询命中恢复（document_id 取 wire id
  双重前缀键、宽松容错），写库通道需要持久身份与显式失败（结构缺陷抛 ValueError，不静默丢
  chunk），故新增严格版转换器；wire→持久→wire 往返恒等有测试。
- 失败语义：worker 不可用或投影缺陷显式失败，经 ``handle_rag_index_event`` 三次重试，不静默
  回退 Python 投影（测试锁定：失败后 KnowledgeIndexEntry 无残留行）。
- 影子漂移守卫：ts 模式下 ``shadow_compare_build`` 基线自动切换为「Python 本地投影 vs TS
  投影」——守卫未来 Python record 方言演进后 TS 适配器漏更的漂移（测试用篡改 adapt 输出验证
  抓得到，且确认已写库产物不回退不纠正）。

## 验证

- 新增 ``tests/test_rag_write_mode.py`` 6 项。核心安全属性：**ts 模式写出的 KnowledgeIndexEntry
  行与 python 模式逐字段一致**（document_id/version/chunk/title/summary/content/content_hash/
  scope/metadata/source_updated_at 全列对比）。
- 回归：后端全量 2336 通过、TS 38 通过、typecheck 通过（本轮无 TS 源码变更，制品无需重建）。

## 待用户确认

启用 = devserver/生产配置 ``rag_write_mode=ts``（或 env ``search__rag_write_mode=ts``），
即时生效于下一次索引写入；回滚 = 切回 python，无需数据迁移。启用建议路径：先 devserver 观察
数日影子基线（mismatch=0）→ devserver 切 ts → 生产跟随。启用后按旧代码清点触发条件重新评估
Python 投影与 KnowledgeIndexEntry 的存留（最终清理的前置）。

# devserver 写路径灰度启用 + 旧 pyrag 代码最终清点（2026-09-09，收尾）

## devserver 启用 rag_write_mode=ts（第③步灰度）

- gugu-backend/gugu-worker systemd drop-in 加 ``search__rag_write_mode=ts`` 重启生效（回滚=
  删 drop-in 文件 + daemon-reload + restart，或切回 python）。
- **真实数据行级验证**：同一用户六来源重建后 4301 行 KnowledgeIndexEntry，python 与 ts 两模式
  的行摘要 sha256 一致（``90a932e10f48d3b0``）。自然业务写入自此走 TS 投影，影子漂移守卫
  （Python 本地投影 vs TS 投影）随 ``rag_write_shadow`` 常开持续记录。
- 生产（112.124.33.116）仍 python；查询主链 ``rag_query_mode`` 交付仍 legacy，但 devserver 已
  启用 ``unified_shadow`` 观察档（见下节）——生产切换与 unified 交付主链切换均为用户保留决策，
  未擅自切换。

## 查询链 unified_shadow 观察档启用 + 合成探针基线（2026-09-09）

- gugu-backend/gugu-worker systemd drop-in 加 ``search__rag_query_mode=unified_shadow``：
  交付仍走 legacy，unified 只在影子侧运行并把 ``unified_equal`` / ``unified_first_diff_index`` /
  ``unified_selected_count`` 写进 ``source_diagnostics["unified"]``（影子失败只记错误类别，
  影子结果永不交付）。回滚=删 drop-in 文件 + daemon-reload + restart。进程环境
  （``/proc/<pid>/environ``）已验证生效。
- **合成探针基线**（查询词程序化取自该用户索引内容的高频词与标题原文，输出只含 sha 前缀与
  统计量，不含查询原文与正文）：
  - u2 七探针：4 equal、1 分歧（legacy 交付 1 条、影子选 0 条、first_diff=0）；
  - u1 单探针取证：1 分歧（legacy 交付 5 条、影子选 3 条、first_diff=2）。
  - 分歧模式一致：影子选中数 ≤ legacy 交付数——与 unified 已知语义差异（worker 侧 confidence
    阈值与 per-source/per-parent 预算裁剪更紧）方向吻合，属灰度评估项而非缺陷。
- 脚本环境异常根因已查明并修复（本轮插桩取证 + 回归测试）：探针侧两表象——①「无 unified 诊断」
  实为汇总打印盲区，诊断一直在写，值是 ``unified_shadow_error=TsSidecarUnavailable``；②裸
  ``RuntimeError: readuntil() called while another coroutine is already waiting`` 是
  ``_request_unlocked`` 超时取消 readline 后 asyncio 流读取者残留的竞态，旧 except 元组
  （BrokenPipe/Connection/ValueError/TimeoutError）未覆盖 RuntimeError，导致连接不关闭、
  客户端带毒运行直到进程重启。修复：RuntimeError 纳入致命错误处理（关闭连接 +
  ``TsSidecarUnavailable``，下次请求自动重生 worker）；回归测试
  ``test_stream_reader_race_closes_connection_and_raises_unavailable``，后端全量 2337 通过。
  修复后探针复跑：裸异常逃逸为 0，失败全部以文档化类别落影子诊断，legacy 交付不受影响。
  运维备注：默认 ``ts_sidecar_timeout_ms=500`` 对冷启动大索引首查偏紧，若真实流量下
  ``record_recall`` 频现该类别，可调高该配置而非改代码。
- 观察档不影响交付行为，legacy 物理删除触发条件不变（灰度全量切 unified 且观察期通过）。

## 生产切换 runbook（2026-09-09 只读侦察落成，未执行，待用户决策）

生产现状（112.124.33.116，v1.1.0 经典 compose 栈 ``gugu-web-main``，部署目录
``/opt/Gugu-web-main/``，2026-09-08 部署）：

- backend/worker/gateway/sandboxd 共用 ``gugu-web-backend:prod`` 镜像；容器 env 无任何
  ``search__`` 覆盖（写 python、查询 legacy，与保留决策预期一致）。
- 镜像内已含 node（v20.19.2）与 RAG worker bundle（``/app/bin/gugu-rag-ts-worker.mjs``，
  构建期 ``--version`` 自检），TS sidecar 具备容器内运行前提。
- **关键前置：v1.1.0 镜像不含 PRD-RAG-7 Phase 4/5 代码**（无 ``rag_write_mode`` 配置项、
  bundle 为方言对齐前版本），对旧镜像注入环境变量无效。生产切 ts/unified 必须先走发版：
  本分支经 PR 合入 main → 人工触发双 workflow 全绿 → 本地预检 → tag → 流水线出镜像 →
  生产拉取 retag prod（走 1ms.run 显式全限定名，直连 docker.io 超时）。
- 切换注入方式：新建 ``/opt/Gugu-web-main/docker-compose.override.yml``（不改动 Git 跟踪的
  ``docker-compose.prod.yml``，不碰受保护的 ``backend/.env``），为 backend+worker 两服务加
  ``environment:`` 段注入 ``search__rag_write_mode=ts``（建议同时 ``search__rag_write_shadow=true``
  开漂移守卫；查询侧先 ``search__rag_query_mode=unified_shadow`` 观察）；然后
  ``docker compose -p gugu-web-main -f docker-compose.prod.yml up -d`` 重建两容器。
  **必须显式 ``-p gugu-web-main``**，否则会拉起第二套含空库的堆栈。回滚=删 override 重建。
- 验证：复用 devserver 行级摘要脚本模式，对生产六来源重建做 python/ts 摘要比对；资源注意：
  生产仅 2 核/1.6G 内存，sidecar node 进程新增常驻内存，切换后观察 OOM/swap 抖动。

## 旧 pyrag 代码最终清点（第二轮，按触发条件执行）

孤儿扫描方法：ast 提取 ``agent/rag`` 全部 238 个顶层定义，按全仓库出现次数（定义行之外零引用）
精筛。**发现并删除 1 项真死代码**：``IndexCache.get_snapshot_documents`` 及其级联
（``_SnapshotDocuments`` 结构、``_snapshot_documents``/``_snapshot_document_locks`` 字段、
``_purge_expired`` 文档过期段、未用 ``typing`` 导入）——snapshot 文档复用缓存自 b08561fe1
引入后从未被接线。删除后后端全量 2336 通过。

存留重新评估（写路径移交已完成，触发条件到点）：

1. **Python record 投影管线：保留**。三重活跃依赖：ts 模式影子漂移守卫的基线投影、
   ``rag_write_mode=python`` 回退路径、memory/knowledge/project 三来源仍由 Python 适配器
   构建（第③步只覆盖六 record 来源）。
2. **KnowledgeIndexEntry 表：保留**。两种模式下都是跨进程持久 chunk 事实源（ts 模式只把
   分块事实来源换为 TS，落库仍经 Python）；查询冷恢复与 ILIKE 兼容路径依赖它。
3. **legacy 查询交付链（UnifiedRetriever 旧多请求路径、Memory/ProjectRetriever 交付、
   hybrid/fusion Python 融合回退）：保留**。删除触发条件「灰度全量切 unified 且观察期通过」
   未达成（生产 legacy、devserver 未切 unified）。

## 收尾状态

- PRD-RAG-7 全部条目：Phase 1-3 完成；Phase 4 三步走全部实施（devserver ts 已启用、生产
  待决策）；Phase 5 unified 可用，devserver 已开 ``unified_shadow`` 观察档（交付仍 legacy，
  切 unified 交付待决策）；唯一剩余 [ ] 为跨灰度门的物理清理项，触发条件与清单均已写入本文件。
- 后续待用户决策的两项：生产 ``rag_write_mode=ts``；``rag_query_mode=unified`` 灰度
  （devserver → 生产）。决策后按上述触发条件执行对应删除即可，无新增实施工作。
