# PRD-RAG-9 增量索引重建与来源级同步实施

PRD：`docs/prds/PRD-RAG-9-增量索引重建与来源级同步.md`

## Phase 0（2026-09-12）：契约冻结 + 基线

### 已落地

- `backend/agent/rag/delta.py`：冻结增量契约——`parent_document_key`、`chunk_slot_key`、
  `document_digest`（覆盖 slot/source_type/title/summary/content/context/chunk_count/scope，
  **不含 version**）、`compute_chunk_delta`（upserts/deletes/unchanged，输出按 slot 键排序确定）、
  `projection_revision_stamp`。
- 契约单源化：`ts_sidecar._worker_document_key` / `_index_document_digest` 与
  `index_cache` 本地同族函数全部改为委托 delta，消除双份实现漂移。
- `index_cache` 的 patch 分支改用 `compute_chunk_delta`（查询侧增量本已存在，替换 digest 逻辑）。
- 诊断扩展（PRD §9）：`record_index_update` 增加 mode / upsert_count / delete_count /
  projection_ms / status / base_revision_match；`rebuild_source_index` 通过显式 `stats_out`
  回传统计，事件处理层记 `mode="source_replace"`（当前唯一路径）。
- `replace_source_documents` 增加 stats 出参（inserted/updated/deleted/elapsed_ms），返回值不变。
- 测试：`backend/tests/test_rag_delta.py` 9 例全绿；RAG 回归 242 例全绿。
  其中测试驱动出两个 digest 缺口并已补：chunk_count（chunk 收缩不可见）、scope（范围变更不可见）。

### 基线数据（devserver，2026-09-12，来源级全量重建，3 轮取中位数）

| 来源 | 规模 | chunk 数 | record 生成 | TS projection | 写库 |
|------|------|---------|------------|--------------|------|
| knowledge | 50 | 50 | 10ms | 13ms | 11ms |
| knowledge | 200 | 200 | 35ms | 46ms | 26ms |
| knowledge | 1000 | 1000 | 182ms | 157ms | 211ms |
| file | 50 | 50 | 17ms | 5ms | 13ms |
| file | 200 | 200 | 42ms | 16ms | 24ms |
| file | 1000 | 1000 | 286ms | 67ms | 192ms |
| project | 50 | 50 | 4ms | 4ms | 10ms |
| project | 200 | 200 | 6ms | 11ms | 22ms |
| project | 1000 | 1000 | 30ms | 53ms | 156ms |

测量脚本：`backend/scripts/bench_rag_rebuild_baseline.py`（内存 SQLite + 临时存储根，
只输出数量与毫秒，不落正文）。Phase 4 用同脚本对比增量路径。

### 基线脚本踩坑

- 初版 owner 每次调用重新生成 → seed 与 measure 数据对不上（空 record）；改为单 owner 贯穿。
- knowledge 是文件库存储（KnowledgeStore 读 .md），不存在 DB 表；种子必须走
  `save_capture`（normalize_capture + KnowledgeStore.save），并把
  `settings.storage.local_path` 指向临时目录（`get_storage()` 每次重读 settings，生效）。
- devserver 上脚本正常退出但 asyncio worker transport 在 loop 关闭后析构，
  打印 `Event loop is closed` 噪音——不影响结果文件，未处理。

### Phase 0 验收核对（PRD §10）

- [x] 同一输入下 diff 结果确定（乱序输入同输出，测试覆盖）
- [x] 正文不进入可见日志（诊断只含计数/毫秒/状态码）
- [x] 旧来源级重建行为不变（242 例 RAG 回归全绿；replace 路径未改语义）

## Phase 1（2026-09-12）：Knowledge 文档级增量

### 已落地

- 单条读取：`KnowledgeStore.get(entry_id)`（按 ID 直读单个 .md，不遍历全库）；
  `KnowledgeAdapter.build_source_record_for(source_id)` 出单条 canonical record。
- 单文档增量写库：`persistent_store.apply_document_patch`——**父文档作用域 replace**：
  该 parent 下不属于 upsert 键集的行一律删除（版本推进/chunk 收缩/正文改写都不残留旧 chunk），
  然后整来源 bump `indexed_at`（owner revision 取 max(indexed_at)，纯删除时必须显式推进，
  否则其他进程查询侧检测不到变化）。
- 向量增量：`agent/knowledge/vector_cache.apply_vector_delta(upserts, delete_keys)`——
  只 upsert 变更 chunk、删指定 key，不做整表 prune（sync_vectors 的 prune 语义在
  增量路径会误删其他文档向量）。删除键来自旧 chunk 的 `cache_key`（含 version，
  不能用 slot key）。
- TS worker patch：`pipeline.update_knowledge_document` 在 DB 提交后主动把单文档增量
  patch 给 worker（整表持久向量搭载），这样下次查询 `reuse_if_current` 直接命中；
  worker 报 `revision_mismatch` → 用 DB 全量投影做 worker 侧来源级 replace 回退；
  worker 不可用（未启动等）只记 `status=worker_unavailable`，DB revision 已推进，
  查询侧懒同步自愈。
- 事件路由：`handle_rag_index_event` 中 knowledge 事件带 `source_id` 走文档级增量，
  否则保持来源级重建。`save/update/delete_knowledge` 工具本来带 source_id；
  自动反思（reflect_if_candidate）改为返回保存条目 id 列表并逐条发事件
  （原来发空 source_id 触发全量重建）。
- 测试：新增 `test_rag_knowledge_delta_index.py` 8 例（单条读取 spy、单条投影、
  正文变更无残留、删除保兄弟、no_change 不打 worker、mismatch 回退、worker 不可用自愈、
  事件路由）；全量后端 2553 例全绿。

### 关键决策

- **DB 侧用「父文档作用域 replace」而不是按 delete 列表删**：chunk slot 键不含 version，
  正文改写时 delta 的 deletes 为空（同槽位由 upsert 覆盖），只有 slot 消失才有 deletes；
  version 推进换行键，必须靠键集差删旧行。
- no_change（digest 全等）不推进 revision、不打 worker——重复事件天然幂等且零成本。
- 测试驱动管线需绕过内存 SQLite 短路：抽成 `pipeline._in_memory_sqlite()` 供
  monkeypatch；且必须同步 monkeypatch `db_session._engine_loop`，否则 ensure_engine
  跨 loop 误判会连回真实数据库（conftest 里踩过的同一个坑）。

### Phase 1 验收核对（PRD）

- [x] 修改 1 条只读取该条（KnowledgeStore.get 单文件读取 + 单 record 投影，测试 spy 覆盖）
- [x] 正文/关键词修改、删除均无旧 chunk 残留（summary 含关键词 → digest 变化触发 upsert；
      DB 父文档作用域 replace 兜底）——恢复场景由再次保存（新条目）自然走 upsert
- [x] 1000 条规模对比在 Phase 4 用基线脚本量化

### 本阶段踩坑

- 自制 DB fixture 忘同步 `_engine_loop`，合跑时 ensure_engine 连回真实 Postgres
  （FK violation 拦下了写入，未造成脏数据）。
- `_full_seed` 没 commit，session 关闭回滚导致后续断言全部空转，前两轮失败全是它。

## Phase 2（2026-09-12）：文件与项目增量

### 已落地

- 发布链透传实体 id：`app/core/events.publish` 聚合 `entity_id`/`entity_ids`/
  `file_op.id|ids` → `_publish_rag_index_events` 逐条发带 `source_id` 的
  文档级 RagIndexUpdated；没带 id 的资源保持来源级事件（全量兜底不变）。
- 单对象读取：`index_builder.build_single_source_record` 支持 file（含正文抽取）
  与 project（复用 ProjectAdapter._project_text）；主数据缺失/已删除/不可索引
  返回 None → 按删除收敛。
- 管线泛化：`update_knowledge_document` 变为 `update_document(user, source_type,
  source_id)` 的兼容别名；knowledge/file/project 共用同一条 patch/回退/worker
  链路；向量增量仅 knowledge 有。事件路由扩到三种来源。
- **父文档定位统一用 source_id 列**：parent_document_id 因来源而异（project 存
  `project:<id>`，file/knowledge 存裸 id），source_id 恒为裸对象 id——这是本阶段
  测试抓出的真 bug，delete 路径按 parent 查会查空导致 no_change 误判。
- 测试：新增 `test_rag_file_project_delta.py` 6 例（覆盖只 patch 自身、删除不影响
  兄弟文件、重命名/移动改 digest、项目字段+阶段变化、项目删除、entity_ids 透传）；
  全量 2559 例全绿。

### 受影响集合定案（PRD 要求明确）

- file record 只含 folder_id/stage_name 等 id 与自身元数据，**不含 folder 名**：
  文件夹改名不改变任何 file record → 受影响集合恒为文件自身。
- project record 只拼接项目自身事实，不依赖文件正文：文件增删改不影响项目 doc。
- 因此移动/重命名/覆盖的受影响集合 = 单对象；旧 scope 残留由 digest 的 scope
  字段变化触发 upsert 兜住（Phase 0 补的 scope 进 digest 在这里兑现）。

### Phase 2 验收核对（PRD）

- [x] 单文件或单项目变更不扫描无关对象（build_single_source_record 单行 select）
- [x] 移动后旧目录/旧项目 scope 不再召回（digest 含 scope 字段，测试覆盖）

## Phase 3（2026-09-12）：剩余来源 + durable recovery

### 已落地

- 单对象读取扩展到 calendar/note/canvas（`build_single_source_record`）：
  - calendar/note：单行 select，note 仅 kind∈{note,suggestion}（与全量构建一致）；
  - canvas：单 item 三表 join + 只加载该节点相关边与端点标题（relation_summary
    与全量构建同源；关系变化影响两个端点，业务事件须带受影响 item id）。
- `DOCUMENT_PATCH_SOURCE_TYPES` 扩到 knowledge/file/project/calendar/note/canvas，
  共用同一条 patch/mismatch 回退/worker 链路。
- durable recovery 核对：`index_jobs`（persist_event/mark_running/mark_result/
  due_events）就是 PRD 所说的「索引 outbox」，`app/main.py` lifespan 已启动
  `start_rag_index_recovery()`（5s 轮询重放 due 任务）——本阶段验证接线并加
  守护测试，不重复造 dirty marker。
- 事件合并/重试/状态查询沿用 bus 既有实现（同源合并保留最新、MAX_RETRIES 退避、
  `get_rag_index_status`）。
- 测试：新增 `test_rag_remaining_sources_delta.py` 5 例；全量 2564 例全绿。

### conversation / memory 的定案（不接单文档增量）

- conversation：record 粒度是「每条消息」且 content_before/after 依赖相邻消息，
  改一条会牵动兄弟 record；文档键也不稳定（baseline_message_id 变化会重排）。
  保持来源级重建。
- memory：走 worker 瞬态槽通道（replace_transient，指纹未变零 IPC），不落
  KnowledgeIndexEntry，delta contract 不适用；快照重建机制已有（revision 戳）。

### 定期来源校准

- Admin 存储对账已有文件库校准；RAG 侧的「来源 vs 索引」校准入口依赖管理端
  新面板，超出本 PRD 骨架范围，按目标约定跳过（不影响正确性：query 侧
  revision 检测 + due 任务重放已保证最终一致）。

### Phase 3 验收核对（PRD）

- [x] 重启/重复事件/事件丢失：durable outbox + 5s 恢复循环 + digest 幂等（既有测试 + 新守护测试）
- [x] 无法恢复时有明确管理诊断：bus 状态机 last_error/index_update_failed + §9 诊断日志

## Phase 4（2026-09-12）：性能对比与收尾

### 单文档变更：文档级增量 vs 来源级全量（devserver 实测，5 轮）

| 规模 | document_patch P50/P95 | source_replace P50/P95 | 加速比 |
|------|------------------------|------------------------|--------|
| 1000 条 | 54ms / 93ms | 418ms / 494ms | ~7.7× |
| 3000 条 | 161ms / 197ms | 1268ms / 1361ms | ~7.9× |

（来源级耗时与 Phase 0 基线一致量级：1000 条全量 ≈ 550ms 含 TS 投影与写库。）

测量脚本 `scripts/bench_rag_delta_compare.py`。文档级耗时里还包含
KnowledgeStore.save 的 O(n) 全库 list（业务写入自身成本），纯索引增量部分
（单条读取 + 单条投影 + patch 写库）只有几十毫秒——增量收益随库规模线性放大。

### 批量事件合并窗口定案

- bus 合并是「同源保留最新 + 完成后重跑」，无时间窗，不会重复 patch；
- 批量操作（entity_ids 多条）逐条发文档级事件，锁按 (user, source, doc) 分离，
  互相并行、互不阻塞；单文档连续变更被 doc 级锁串行且 no_change 幂等短路。
- 结论：无需引入时间窗参数（PRD 关切的两个失效模式都不存在）。

### 默认增量覆盖确认

- knowledge/file/project/calendar/note/canvas：事件带 source_id 即走文档级；
  文件库 API 的 remove/批量删除已带 file_op id/ids（files.py 5 处）。
- 无 id 的事件（老调用方、管理端触发）自动回退来源级重建，行为不变。
- 定期校准入口：依赖 Admin 新面板，按目标约定跳过（见 Phase 3 记录）。

### 旧路径清理

- 契约无重复实现：`_worker_document_key`/`_index_document_digest` 全部委托
  `agent.rag.delta`（index_cache、ts_sidecar、batch_retriever 共用一份）。
- shadow 路径：rag_query_mode 早已 unified 单档（RAG-7 收口），无遗留。
- chunk diff 唯一入口 `compute_chunk_delta`；查询侧 index_cache 与写侧
  update_document 共用。

## 测试报告（PRD §11 / §14 完成标准核对）

执行日期：2026-09-12，环境：本地 Mac（单测）+ devserver（性能实测）。

### 测试执行结果

| 套件 | 结果 |
|------|------|
| 后端全量（pytest tests/） | **2564 passed**（89s，0 failed） |
| PRD 专项四套件（delta / knowledge / file-project / 剩余来源） | **28 passed** |
| TS worker 协议测试（worker.protocol.test.ts，含 patch 原子性/并发/mismatch 26 例） | **26 pass / 0 fail** |
| 性能对比（devserver 实测） | 见 Phase 4 表格 |

### §11 测试计划逐项覆盖

- 单文档增/改/关键词/版本/删除/恢复：knowledge 8 例 + file/project 6 例（版本推进、
  关键词改 summary 触发 upsert、恢复=再保存走 upsert）
- chunk 数增/减/前移：test_rag_delta（shrink→deletes、扩容→新 slot upsert、乱序确定）
- 同源事件并发合并不丢更新：bus 既有测试 + 事件路由测试（Phase 1/2 套件）
- 重复事件幂等 / no-op：no_change 测试（不打 worker、不推进 revision）
- projection 失败 / TS patch 失败 / revision mismatch / worker 重启：
  mismatch 回退 replace、worker_unavailable 懒同步自愈测试；worker 重启恢复由
  index-cache-service / worker.protocol 既有 TS 测试覆盖
- owner/scope 隔离：全部增量操作按 owner 收窄（既有 owner 隔离测试继续覆盖）
- 日志脱敏：诊断只含 mode/count/ms/status 固定枚举（§9 字段）
- 向量失败不影响词法：apply_vector_delta 静默降级（try/except 返回 0）
- 性能五场景：单条修改/删除对比表；连续变更=doc 级锁串行 + no_change 短路；
  worker 重启恢复由 TS 测试与 restore_error 自愈覆盖；全量基线见 Phase 0 表

### §14 完成标准逐条

1. ✅ Knowledge 单文档变更只读取该条（KnowledgeStore.get 单文件 + spy 测试）
2. ✅ 修改/删除/移动后旧 chunk 与旧 scope 均清理（父文档作用域 replace + digest 含 scope）
3. ✅ mismatch/重启/重复事件有回归测试（Python 3 例 + TS 26 例）
4. ✅ 同源并发不重复启动任务、不丢最终状态（bus 合并 + pending 重跑，既有保证）
5. ✅ 索引失败不影响业务提交（事件 fire-and-forget + outbox 重放 + 来源级回退）
6. ✅ session snapshot / 权限 / 排序 / 上下文前缀不变（查询侧只改了 digest 实现，
   242 例 RAG 召回回归全绿）
7. ✅ 诊断无正文/附件/凭据/路径
8. ✅ 全量 vs 增量性能对比已记录（本文 Phase 0 / Phase 4）

### 结论

PRD-RAG-9 Phase 0-4 全部实施并验证通过；六来源默认走文档级增量，
来源级重建保留为兜底/管理入口，具备 `mode` 诊断与明确回滚路径（§13）。
