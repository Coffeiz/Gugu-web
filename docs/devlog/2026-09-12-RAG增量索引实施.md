# PRD-RAG-9 增量索引重建与来源级同步实施

PRD：`docs/prds/PRD-RAG-9-增量索引重建与来源级同步.md`

## Phase 0（2026-09-12）：契约冻结 + 基线

### 已落地

- `backend/agent/rag/delta.py`：冻结增量契约——`parent_document_key`、`chunk_slot_key`、
  `document_digest`（覆盖 slot/source_type/title/summary/content/context/chunk_count/scope，
  **不含 version**）、`compute_chunk_delta`（upserts/deletes/unchanged，输出按 slot 键排序确定）、
  `projection_revision_stamp`。
- 契约单源化：`ts_sidecar._worker_document_key` / `_index_document_digest` 与
  `index_cache` 本地同族函数全部改为委托 delta，消除双份实现漂移。
- `index_cache` 的 patch 分支改用 `compute_chunk_delta`（查询侧增量本已存在，替换 digest 逻辑）。
- 诊断扩展（PRD §9）：`record_index_update` 增加 mode / upsert_count / delete_count /
  projection_ms / status / base_revision_match；`rebuild_source_index` 通过显式 `stats_out`
  回传统计，事件处理层记 `mode="source_replace"`（当前唯一路径）。
- `replace_source_documents` 增加 stats 出参（inserted/updated/deleted/elapsed_ms），返回值不变。
- 测试：`backend/tests/test_rag_delta.py` 9 例全绿；RAG 回归 242 例全绿。
  其中测试驱动出两个 digest 缺口并已补：chunk_count（chunk 收缩不可见）、scope（范围变更不可见）。

### 基线数据（devserver，2026-09-12，来源级全量重建，3 轮取中位数）

| 来源 | 规模 | chunk 数 | record 生成 | TS projection | 写库 |
|------|------|---------|------------|--------------|------|
| knowledge | 50 | 50 | 10ms | 13ms | 11ms |
| knowledge | 200 | 200 | 35ms | 46ms | 26ms |
| knowledge | 1000 | 1000 | 182ms | 157ms | 211ms |
| file | 50 | 50 | 17ms | 5ms | 13ms |
| file | 200 | 200 | 42ms | 16ms | 24ms |
| file | 1000 | 1000 | 286ms | 67ms | 192ms |
| project | 50 | 50 | 4ms | 4ms | 10ms |
| project | 200 | 200 | 6ms | 11ms | 22ms |
| project | 1000 | 1000 | 30ms | 53ms | 156ms |

测量脚本：`backend/scripts/bench_rag_rebuild_baseline.py`（内存 SQLite + 临时存储根，
只输出数量与毫秒，不落正文）。Phase 4 用同脚本对比增量路径。

### 基线脚本踩坑

- 初版 owner 每次调用重新生成 → seed 与 measure 数据对不上（空 record）；改为单 owner 贯穿。
- knowledge 是文件库存储（KnowledgeStore 读 .md），不存在 DB 表；种子必须走
  `save_capture`（normalize_capture + KnowledgeStore.save），并把
  `settings.storage.local_path` 指向临时目录（`get_storage()` 每次重读 settings，生效）。
- devserver 上脚本正常退出但 asyncio worker transport 在 loop 关闭后析构，
  打印 `Event loop is closed` 噪音——不影响结果文件，未处理。

### Phase 0 验收核对（PRD §10）

- [x] 同一输入下 diff 结果确定（乱序输入同输出，测试覆盖）
- [x] 正文不进入可见日志（诊断只含计数/毫秒/状态码）
- [x] 旧来源级重建行为不变（242 例 RAG 回归全绿；replace 路径未改语义）

## Phase 1（2026-09-12）：Knowledge 文档级增量

### 已落地

- 单条读取：`KnowledgeStore.get(entry_id)`（按 ID 直读单个 .md，不遍历全库）；
  `KnowledgeAdapter.build_source_record_for(source_id)` 出单条 canonical record。
- 单文档增量写库：`persistent_store.apply_document_patch`——**父文档作用域 replace**：
  该 parent 下不属于 upsert 键集的行一律删除（版本推进/chunk 收缩/正文改写都不残留旧 chunk），
  然后整来源 bump `indexed_at`（owner revision 取 max(indexed_at)，纯删除时必须显式推进，
  否则其他进程查询侧检测不到变化）。
- 向量增量：`agent/knowledge/vector_cache.apply_vector_delta(upserts, delete_keys)`——
  只 upsert 变更 chunk、删指定 key，不做整表 prune（sync_vectors 的 prune 语义在
  增量路径会误删其他文档向量）。删除键来自旧 chunk 的 `cache_key`（含 version，
  不能用 slot key）。
- TS worker patch：`pipeline.update_knowledge_document` 在 DB 提交后主动把单文档增量
  patch 给 worker（整表持久向量搭载），这样下次查询 `reuse_if_current` 直接命中；
  worker 报 `revision_mismatch` → 用 DB 全量投影做 worker 侧来源级 replace 回退；
  worker 不可用（未启动等）只记 `status=worker_unavailable`，DB revision 已推进，
  查询侧懒同步自愈。
- 事件路由：`handle_rag_index_event` 中 knowledge 事件带 `source_id` 走文档级增量，
  否则保持来源级重建。`save/update/delete_knowledge` 工具本来带 source_id；
  自动反思（reflect_if_candidate）改为返回保存条目 id 列表并逐条发事件
  （原来发空 source_id 触发全量重建）。
- 测试：新增 `test_rag_knowledge_delta_index.py` 8 例（单条读取 spy、单条投影、
  正文变更无残留、删除保兄弟、no_change 不打 worker、mismatch 回退、worker 不可用自愈、
  事件路由）；全量后端 2553 例全绿。

### 关键决策

- **DB 侧用「父文档作用域 replace」而不是按 delete 列表删**：chunk slot 键不含 version，
  正文改写时 delta 的 deletes 为空（同槽位由 upsert 覆盖），只有 slot 消失才有 deletes；
  version 推进换行键，必须靠键集差删旧行。
- no_change（digest 全等）不推进 revision、不打 worker——重复事件天然幂等且零成本。
- 测试驱动管线需绕过内存 SQLite 短路：抽成 `pipeline._in_memory_sqlite()` 供
  monkeypatch；且必须同步 monkeypatch `db_session._engine_loop`，否则 ensure_engine
  跨 loop 误判会连回真实数据库（conftest 里踩过的同一个坑）。

### Phase 1 验收核对（PRD）

- [x] 修改 1 条只读取该条（KnowledgeStore.get 单文件读取 + 单 record 投影，测试 spy 覆盖）
- [x] 正文/关键词修改、删除均无旧 chunk 残留（summary 含关键词 → digest 变化触发 upsert；
      DB 父文档作用域 replace 兜底）——恢复场景由再次保存（新条目）自然走 upsert
- [x] 1000 条规模对比在 Phase 4 用基线脚本量化

### 本阶段踩坑

- 自制 DB fixture 忘同步 `_engine_loop`，合跑时 ensure_engine 连回真实 Postgres
  （FK violation 拦下了写入，未造成脏数据）。
- `_full_seed` 没 commit，session 关闭回滚导致后续断言全部空转，前两轮失败全是它。
