# MVP 目标文档

> 版本：0.11.1　|　更新：2026-06-24
> 阶段：Alpha 已完成 → **MVP 内测准备中**

---

## 一、MVP 定义

**目标用户**：有目标要推进、想做个人成长记录的个人用户（工作 / 创作 / 学习 / 生活皆可，创作者是重点群体之一），通过邀请码小规模内测（10–50 人）。

**MVP 成功标准**：
- 用户能独立完成「新建项目 → 归档文件 → 用咕咕管理进度 → 收到截稿提醒」完整主线
- 稳定运行 ≥ 2 周无服务中断
- 收到有效反馈 ≥ 20 条，能驱动下一个迭代方向

**MVP 不做的事**：
- 不追求完整功能覆盖（思维画布、素材板、ToB 均不是 MVP 范围）
- 不追求极致性能优化（够用即可）
- 不开放公开注册（邀请码管控）

---

## 二、功能节点

### ✅ 已完成（Alpha 交付）

| 模块 | 关键能力 | 状态 |
|------|---------|------|
| 用户系统 | 邀请码注册 / JWT / 头像 / 个人设置 | ✅ |
| 项目管理 | 看板 / 阶段 / 待办 / 优先级 / 进度 / 颜色 / 归档 | ✅ |
| 日历 | 月视图 / 项目排期 / 自定义事件 / 节假日 | ✅ |
| 文件库 | 四空间 / 上传下载 / 预览 / 缩略图 / 回收站 / 批量操作 | ✅ |
| 存储后端 | 本地磁盘 / OSS 双后端 / Admin 热切换 / OSS 预签名直传 | ✅ |
| AI Agent | 23 工具 / 流式 / 记忆系统 / 人格 / 配额 / 删除确认 | ✅ |
| IM 接入 | 飞书 + QQ + 微信 BYO 自连 / 文件收发 / PDF·Office 读取 / 音视频·语音 | ✅ |
| 实时同步 | Redis pub/sub → SSE，IM 操作推网页 | ✅ |
| 用户反馈 | FeedbackModal / Admin 反馈页 / SMTP 邮件通知 | ✅ |
| Admin 后台 | 配置热更新 / 用量统计 / 用户管理 / 审计日志 / 对账工具 | ✅ |

### 🔲 MVP 前需完成

| 项目 | 说明 | 优先级 |
|------|------|--------|
| **客户管理页面** | 后端 + Agent 工具已就绪，只缺前端页面；创作者常有固定客户，属主线功能 | 高 |
| **定时任务 + 截稿通知** | APScheduler 定时扫描到期项目 → 主动 DM 用户（IM 出口已就绪）；把咕咕从「被动工具」做成「会惦记你的伙伴」，MVP 核心差异点 | 高 |
| **新用户引导** | Onboarding 已删，现靠咕咕首条问候；需确认空状态各页面友好，无明显「不知道从哪开始」的断层 | 中 |

---

## 三、上线前检查清单

### 3.1 基础设施

- [ ] **systemd 三服务**：`systemctl status gugu-web gugu-worker gugu-supervisor` 均为 `active (running)`；`Restart=always` 已配置
- [ ] **开机自启**：`systemctl is-enabled` 三服务均为 `enabled`
- [ ] **nginx**：`/` 指向前端 dist、`/api/` 反代后端 8000、`/uploads/` **不对外暴露**（应由后端鉴权接口提供）
- [ ] **HTTPS**：SSL 证书有效，自动续签（Let's Encrypt）
- [ ] **防火墙**：8000 端口不对外暴露，只 nginx 内网转发

### 3.2 数据安全

- [ ] **数据库备份**：`pg_dump` 每日定时任务，备份文件存异地（对象存储 / 另一台机器）
- [ ] **uploads 目录备份**：本地存储时 `uploads/` 同步到 OSS 或另一台机器；OSS 后端时开启 OSS 版本控制
- [ ] **数据库密码**：非默认值，`.env` 文件权限 `600`
- [ ] **Admin 密码**：`admin123` 已修改
- [ ] **JWT Secret**：非默认，足够随机（≥ 32 字符）

### 3.3 邮件 / 通知

- [ ] **SMTP 配置**：Admin → 系统配置 → 邮件系统 → 测试发送，确认能收到
- [ ] **反馈通知**：提交一条测试反馈，确认邮件送达
- [ ] **收件人地址**：`to_addr` 填的是真实能收信的地址

### 3.4 存储

- [ ] **OSS 切换**（如准备好上线即用 OSS）：Admin 切 `backend=oss` → 上传一张图 → 确认 presign → PUT → confirm 完整链路
- [ ] **本地文件迁移**（如从本地切 OSS）：用 `ossutil` 将 `uploads/` 全量同步到 OSS 同名 key，再切后端
- [ ] **存储对账**：Admin → 数据库 → 存储对账，确认无幽灵/孤儿异常

### 3.5 用户体验

- [ ] **邀请码准备**：生成足够数量（首批用户数 × 1.5），分发渠道确认
- [ ] **空状态验收**：用新账号走一遍主线，检查无数据时各页面显示是否友好
- [ ] **移动端基础可用**：主要功能在手机浏览器可访问（不要求完美，但不能完全崩掉）
- [ ] **404 / 错误页**：已有咕咕风格 404 页，确认路由不存在时正常跳转

### 3.6 监控 / 应急

- [ ] **日志可查**：`tail -f logs/gugu-web.log` / `gugu-worker.log` 能正常输出；Admin → 调试 → 系统日志可用
- [ ] **磁盘空间**：确认服务器磁盘余量充足（uploads 目录会持续增长）
- [ ] **内存 / CPU 基线**：空载状态下确认三服务资源占用在预期范围内
- [ ] **应急预案**：知道怎么快速回滚（保留上一个可用的 git tag / docker image）

---

## 四、内测运营节点

| 节点 | 动作 |
|------|------|
| **D-3 上线前** | 完成上方清单所有 ✅；生产环境压测（手动并发上传、Agent 多轮对话） |
| **D-Day 上线** | 发放首批邀请码（5–10 人）；告知用户反馈入口在左下角用户菜单 |
| **D+3** | 查看 Admin 反馈页 + 系统日志；主动问首批用户卡在哪里 |
| **D+7** | 整理反馈，锁定第一个迭代方向；扩大到第二批用户 |
| **D+14** | 评估稳定性；决定是否继续邀请制或推进 Public Beta |

---

## 五、内测期迭代候选（按反馈优先级排）

以下功能已具备技术基础，内测期间根据反馈密度决定做哪个：

1. **截稿提醒推送** — 高价值，IM 出口现成，APScheduler 接入成本低
2. **客户管理前端** — 有客户的用户会立刻用到
3. **空状态引导优化** — 取决于反馈中「不知道从哪开始」的比例
4. **移动端适配增强** — 取决于用手机访问的用户比例
5. **错误监控（Sentry）** — 取决于 bug 发现速度是否跟不上

---

## 六、不做（明确排除出 MVP）

| 功能 | 排除原因 |
|------|---------|
| 思维画布 | 方向性决策，不影响主线 |
| 素材板 | 同上 |
| 报表与导出 | 内测期数据量不足，需求未验证 |
| 桌面客户端 | 工程量大，Web 够用 |
| ToB / 多租户 | 需要有团队客户需求才启动 |
| Casdoor / SSO | ToB 前置依赖，不在 MVP 范围 |
| OSS 分片上传 | 现有上传够用，大文件场景未验证 |
