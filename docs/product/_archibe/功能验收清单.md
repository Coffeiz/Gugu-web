# 功能验收清单（P1-b / P2 重构 + 本轮改动）

> 用途：在开发机走一遍标准 UI 路径做人工验收。typecheck/单测**不算**交互页验收——这份清单才是。
> 约定：`[ ]` 未验 / `[x]` 通过 / `[!]` 有问题（旁边写现象）。每完成一刀重构就跑对应段。
> 关联：代码审查 [docs/security/代码审查-GPT复审核实版-2026-07-10.md] §6 执行计划；纯函数护栏 `src/utils/projectStages.test.ts`。

---

## A. 项目阶段 / 待办（P2 第一刀迁移的核心，风险最高）

> 迁移 `projectStages.ts` 到**前端** Store → 新建弹窗 → ProjectModal 后，**每迁一处都跑这一段**。目标：行为与迁移前逐字一致。（后端 Agent 不迁 TS，见 C 段。）

**阶段推进**
- [x] 看板卡片右侧「推进」按钮：pending→进行中→已完成，状态与列位置正确
- [x] 卡片阶段条（SegBar）点击某段：当前阶段、进度条、百分比正确
- [x] ProjectModal 里阶段切换：前进/后退，进度联动

**待办勾选**
- [x] 卡片阶段弹层里勾/取消待办：即时反映，进度按「已完成/总数」更新
- [x] ProjectModal 待办勾选同上

**自动完成（快照）**
- [x] 拖卡片到「已完成」列：所有阶段未完成待办被自动勾上（视觉全勾）
- [x] 推进到最后阶段且进度满：同上自动收尾
- [x] setStage 前进跨阶段：经过的阶段待办被自动勾上

**撤销完成（还原快照）——最容易回归的一处**
- [x] 从「已完成」拖回进行中：**自动勾的待办恢复原状**（原来没勾的变回没勾），**用户真手动勾过的仍保持勾**
- [x] 阶段从后往前退：对应阶段的自动完成被还原、真完成保留

**落库（刷新持久化）**
- [x] 上面每类操作后 **刷新页面**：状态/进度/待办勾选与刷新前一致（确认 API 落库、非仅本地）
- [x] 并发锁：两个标签页改同一项目，后改的收到 409 提示并自动刷新（不静默丢失）

**阶段编辑 / 拖动**
- [x] ProjectModal 阶段拖动重排：顺序、进度、待办跟随正确
- [x] 增/删阶段：currentStage 不越界

## B. 新建项目 / 模板（normalizeStages 迁移点）

- [x] 新建项目：默认阶段（继承上个项目 / 兜底「计划·执行·交付」）正确
- [x] 应用阶段模板：阶段+待办按模板填充，key 不冲突
- [x] 新建弹窗里加/删待办、编辑待办文本：保存后进卡片一致
- [x] 阶段名带非法字符时的校验仍生效

## C. Agent 工具建项目（后端 Python 独立实现，靠 API 契约 + 对称测试与前端语义一致——非同一份代码）

- [x] 让咕咕新建带阶段/待办的项目：落库结构与手动建的一致
- [x] 咕咕推进阶段 / 改待办：与前端手动操作行为一致

---

## D. 本轮已完成改动（回归确认，一次性过）

**头像裁切上传（commit b99b25c）**
- [x] 个人资料点头像 → 选长方形图 → 弹裁切框
- [x] 圆形遮罩「外暗内透」预览；拖动 + 滚轮/滑块缩放，圆比框小一圈能看到裁掉部分
- [x] 确定后头像更新；`uploads/avatars/` 下只有一个 `{id}.webp`、几十 KB（不存原图）
- [x] 再传张 PNG：旧 `.webp` 被覆盖，无多余后缀残留

**仪表盘小日历跳转（P3，commit 5232250）** — ⏸ 推迟，以后再验
- [ ] 仪表盘小日历点某天 → 跳完整日历、停在该天所在月、选中该天
- [ ] 点相邻月的灰格 → 跳到它所属的月份（顺带修的 d.iso bug）
- [ ] 小日历假日/调休徽章（休/班）显示正常（同一 bug 影响面）

**时区归属（P1-c）**
- [x] 咕咕「今天是…」按本机时区（非服务器）；本地午夜前后不错一天
- [x] 日历「今天起的事件」、深夜时段判断按用户时区
- [x] 后台 admin 时间显示按浏览器本地时区

**聊天消毒回归（P0 修复的副作用，commit d93ac72）**
- [x] 聊天代码块「复制」按钮可点、真复制到剪贴板
- [x] 咕咕回复里的 `gugu://` 动作链接（连 QQ / 打开文件）可点触发
- [x] 刷新后历史消息里这两样仍生效

**Admin 页（P2 步骤①类型化，行为不应变）**
- [x] Admin/Agent 页各区块（状态标签/轨迹/预设/画像/精力统计图）显示与操作正常
- [x] 其余 Admin 页（用户/服务/审计/配置/通知/感知/系统日志/配额）无白屏、增删改正常

---

## F. 文件域 P2④ 整批（纯函数 / fetchStorage / optimisticMutation / 后端 keys.py / contents 类型化 / 导航 navPathFor）

> 关联：乐观更新盘点见 [docs/refactor/文件域乐观更新盘点.md]；护栏 `src/utils/{fileSize,folderKeys,fileParse,optimisticMutation,filesNav}.test.ts`、`backend/tests/test_storage_keys.py`。
> 全批都是等价替换 / 一致性修复，**行为不应变**，重点验「没回归」+「配额即时更新」+「断网回滚」。
> 自动化门禁（typecheck0·strict0·前端163测试·后端199测试·ownership/confirm 两守卫）已核对全绿——但那不算交互验收，下面才是。
> 部署：本地改动经 Mutagen 同步到 devserver；Web 改动用 `make dev-web` 自动重载，Worker 改动用 `make dev-worker` 自动重载（首次先 `make deps-dev`，且不要和同机 systemd worker 并行）。涉及迁移的改动仍需单独执行 `make migrate`，并确认无 import 启动报错。

**F1. 纯函数抽取不回归（fileSize / folderKeys / fileParse，commit `f34a133`/`e28c74e`/`0e6bccf`）**
- [x] 顶部存储用量文字（已用 X / 上限 Y）格式正常（`fmtBytes`）
- [x] 多选文件夹后移动/删除/剪切：真的作用到所选文件夹（`resolveFolderIds`，"f:65"→数字 id 不落空）
- [x] 「已完成」项目按年/月分组的文件夹显示正确（`doneYear/doneMonth`）
- [x] 上传文件生成的幽灵卡「文件名 + 扩展名」拆分正确（`splitName`，含无扩展名/隐藏文件）

**F2. 存储用量刷新一致（commit `c15c880`）——顶部配额条应「即时」更新，不用刷新页面**
- [x] 删单个文件 / 批量删 / 删文件夹：配额用量**立即下降**
- [x] 回收站还原文件 / 批量还原：配额用量**立即回升**
- [x] 复制粘贴文件：配额用量**立即上升**
- [x] 永久删除 / 清空回收站：用量不变（本就不计已删文件，符合预期）

**F3. 乐观更新迁移不回归（commit `916ee98`，5 处走 optimisticMutation）**
- [x] 删文件（卡片 / 右键）：视图**立即**移除；断网时删除失败 → 文件**自动回滚**回列表
- [x] 拖文件 / 文件夹进目标夹：立即移动；失败 → 回滚到原位
- [x] 剪切 + 粘贴到别处：立即移动；失败 → 回滚（剪贴板已清空的情况下位置还原）
- [x] 以上失败路径都不报错弹窗但控制台有日志、界面状态与后端最终一致

**F4. 后端 keys.py 收敛（commit `2952f40`，纯移动·存量数据边界）**
- [x] 四空间（个人/项目/思维/素材）上传：storage key 路径与迁移前一致，落盘位置不变
- [x] 同名文件上传：冲突改名 `名(1)` 仍生效（`_resolve_conflict`）
- [x] 已有历史文件仍可读/下载/预览（key 逻辑未变，存量不受影响）
- [x] 回收站还原、咕咕（Agent）建/存/移文件：行为一致（trash.py / agent 改了 import）

**F5. 导航路径 navPathFor（commit `d790bd0`，等价替换，重点验「没回归」）**
- [x] 个人 / 项目 / 回收站根入口互相切换正常
- [x] 项目文件 → 状态 → 已完成 → 年 → 月 → 项目 逐层进入，面包屑正确
- [x] 从「月」层进项目：面包屑保留 已完成/年/月 上下文
- [x] 文件夹进子文件夹（个人根 / 项目根 / 嵌套三种起点）路径正确
- [x] 面包屑点段跳回、前进/后退、刷新后恢复正常（历史栈 / sessionStorage 未动）

**F6. 类型化 & 不并入点保持原样（回归确认）**
- [x] `contents` 标类型（`files:FileMeta[]` / `folders:FolderCard`，commit `1952372`）为 type-only：列表/选择/框选/拖拽运行时无变化
- [x] 批量删（含文件夹）、单删文件夹、重命名、回收站永久删/清空、复制粘贴 —— 行为与重构前一致
- [x] 后端红线不受影响：`get_owned()` 授权、删除进回收站 30 天可还原、永久删/清空确认门仍在

**F7. 导航状态搬进 useFilesNav（子步 b，commit `c77512c`，纯搬响应式状态·测试兜不住·必须交互验）**
> 回退点 `92eab9f`——任一条不对即 `git revert c77512c`（只动 useFilesNav.ts + index.vue 两文件）。
- [x] 前进/后退按钮：多层进入后连点后退→前进，位置与内容都对，不打转、不重复记历史
- [x] 面包屑点任意段跳回，跳回后再进别处，历史正确
- [x] 进入文件夹/项目：navPath、内容、选择清理正常
- [x] 刷新页面：导航位置恢复（sessionStorage）
- [x] 删文件夹：历史里引用被删文件夹的帧被剪掉（后退不会退到已删目录，`pruneHistoryForFolders`）
- [x] 顶栏搜索点文件/文件夹跳转：定位到目标目录、面包屑正确（jumpToTarget 仍改返回的 navPath ref）

**F8. 项目分组聚合 projectFolderCards（子步 c 安全子集，commit `ff6a6da`，等价替换）**
- [x] 项目文件 → 状态分组（待开始/进行中/已完成）数量正确、空组不显示
- [x] 已完成 → 年份文件夹（降序）数量正确
- [x] 年 → 月份文件夹（升序）数量正确
- [x] 进入各分组后内容正确（回归确认）
> loadContents 剩余部分为交互主循环，按计划有意不抽（store 编排/派发器）。子步 c 到此为止。

## E. 后续（做到时再补）

- [ ] P2-b 异常收敛后：外部 I/O 失败路径（网络断、文件缺、SDK 错）提示合理，不吞编程错误
