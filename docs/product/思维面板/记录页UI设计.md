# 思维面板 · 记录页 UI 设计

> 状态：定稿 v2（2026-07-10 横置改版——第一版双列 grid 上线后实测观感偏空，改为横置日期列）  
> 上游：[设计草案.md](设计草案.md)（产品定位/内容块/触发键契约，权威）、[实现方案.md](实现方案.md)（工程清单）  
> 范围：只覆盖**记录页**；画布（P2）的 UI 届时另开小节或单独文档  
> 最后更新：2026-07-10

## 一句话基调

**这一页比全站其他页面更"纸"**——胶囊条和捕捉条留住玻璃语言跟全站呼应，中间整个内容区是哑光的纸片流，玻璃只出现在边缘 chrome 上。承接设计草案"纸条表示人的思考、玻璃表示系统对象"的双语言：P2 画布进来时（纸条贴纸 + 玻璃系统对象）视觉语言是连续的。

## 为什么横置（v2 改版记录）

第一版是纵向双列 grid + 右侧日期索引。真实数据一进来就露馅：便签多为短句，横跨半屏的宽卡片装一行字，又空又扁；右上角的小日期索引悬在大片空白旁边很尴尬。改为**一天一列、时间横向流动**后：短便签在 280px 窄列里天然紧凑；"单日单条右格空着"的问题直接消失（每天独占一列，没有格子要配对）；横置画面跟 P2 画布的"工作台"气质连续。参考形态：周视图日历（列=天、列内竖排条目）。

## 整体骨架（full-bleed，纵向四层）

`/mind` 走 full-bleed（路由 `meta.fullBleed`，DefaultLayout 据此隐藏 topbar、去掉顶部渐变遮罩、`page-content` 收窄留白且不滚动，滚动交给页面自管）。topbar 的 GlobalSearch 由胶囊条右侧的便签筛选补位。

```
[侧栏] ┌──────────────────────────────────────────────┐
       │            ( 记录 ◉ | 画布 )      [🔍 筛选]   │ ← 胶囊椭圆、水平居中
       │  [今天·6] [7月9日·1] [7月8日·5] …             │ ← 日期条（横向 chips）
       │  ┌ 今天 周五 6 ┐ ┌ 7月9日 ┐ ┌ 7月8日 ┐       │
       │  │ [卡]        │ │ [卡]   │ │ [卡]   │  →横滚 │ ← 一天一列，280px
       │  │ [卡]        │ │        │ │ [卡]   │       │    左新右旧
       │  │ [卡] ↓列内滚 │ │        │ │ [卡]   │       │
       │  └─────────────┘ └────────┘ └────────┘       │
       │        ┌ 记点什么…              ➤ ┐           │ ← 捕捉条，居中停靠
       └──────────────────────────────────────────────┘
```

| 层 | 规格 | 玻璃？ |
|---|---|---|
| 胶囊条 | 椭圆（全圆角 999px）、**水平居中**（grid 三列布局保证真居中），筛选框靠右同排 | 可以（背后静态） |
| 日期条 | 横向 chips（`今天·6`），滚动联动高亮、点击横向跳列、高亮 chip 自动带回可视区 | 否——裸 chips |
| 便签流 | 横向滚动，一天一列 280px，**左新右旧**（今天最左）；无便签的日期**不出列**（时间轴压缩） | 否——卡片是纸 |
| 捕捉条 | 视口底部停靠、**水平居中**（与胶囊呼应，max 680px）；收起 44px 单行，聚焦展开 | 玻璃面板，内部交互克制 |

**玻璃红线适用**（design.md「弹窗动画规范」+ devlog 07-01/07-06）：捕捉条浮在滚动内容之上，内部交互元素 hover 一律走 opacity 叠层，不动 background/box-shadow（白带）；任何玻璃元素的祖先链不挂 opacity/filter/mask 动画（隔离组）。

## 滚动模型（横竖两轴分工）

- **横轴 = 翻历史**：纵滚轮在列外/无溢出列上时转为横滚（`wheel` 事件 `scrollLeft += deltaY`）；触控板横扫、Shift+滚轮走浏览器原生；日期条点击直达。横向滚动条隐藏（导航靠滚轮/日期条，露在捕捉条底下很脏）。
- **竖轴 = 翻当天**：列内溢出时列内自己竖滚（细滚动条）；滚轮悬在有溢出的列上优先列内竖滚。
- **日期条联动**：IntersectionObserver 盯 `.tl-col[data-date]`（root=横滚容器，rootMargin 右侧收 60%），当前=左带里最靠左的列；高亮 chip 滚出日期条可视区时 `scrollIntoView` 带回。

## 便签卡片（主角，纸不是玻璃）

- **底色近实底**：`rgba(255,255,255,0.82)`，**不用 backdrop-filter**——纸的质感本该实，顺带绕开白带/隔离组两个玻璃坑，列内让位动画可放心用 transform（TransitionGroup move）。
- **借薄不借悬浮**：radius 10px、`1px solid rgba(0,0,0,0.06)` 描边、padding 11px 12px、无投影；hover 走 `::after` + 白 50% opacity 淡入。
- **卡内结构**：正文 13px/1.6；长内容 line-clamp 6 行 + 「展开」（阅读态展开，不进编辑）；**时间戳常驻**左上（当天只显 HH:MM，往日/补录显完整日期）；待办渲染成可勾 checkbox **卡上点击即完成**（直接 PATCH 走乐观锁，不进编辑）；对象引用小 chip；**咕咕代记微标**加工档才标（`record_note` 的 origin 字段，直录不标）。
- **可选颜色**：整卡 ~8% 低饱和 tint（紫灰/粉灰/青灰/暖黄），不做左侧色条。
- **编辑 = 列内就地展开**（v2 改：不再跨列）：280px 列里卡片原地变编辑器（Trello 卡片式），工具栏藏掉 `@` 提示文字（放不下，捕捉条那份还在）；同列其余卡由 TransitionGroup move 让位。

## 捕捉条（底部停靠、居中）

- 收起态：单行 44px「记点什么…」+「随手记」角标；**收起不丢草稿**（单行里露草稿开头提醒"还有没记完的"）。
- 聚焦展开：编辑器 + 工具栏（正文/单级标题/待办/列表 + `@` 引用）从条内长出来，不弹浮层；补录日期选择器同排。
- 编辑器行为遵循设计草案：无 Markdown 输入规则、格式只走工具栏、`@` 引用（行首/空白前触发、连续无结果自动关）、`/` 预留咕咕。

## 提交行为

- **常规提交**：若已横滚进历史 → 先平滑滚回最左（今天列）→ 新卡插列顶 + 紫灰 12% 高亮 ~1.8s 淡出。
- **补录**（`captured_at` 是过去）：**不滚动**，toast「已记到 7 月 3 日」。不做这条反馈就是 bug——补录落进右边很远的列，眼前没有任何动静，用户会以为没保存。

## 空态

**未来融合进新手引导**（onboarding 系统）：P1 先放最简占位（"还没有记录，在下面记一条试试～"指向捕捉条），完整引导（这页是干嘛的 + "或者在 QQ 上对咕咕说『记一下』"）挂进新手引导流程时一并做。

## 遗留 / 画布预告

- 画布（P2）UI 规格届时补：full-bleed 点阵背景、纸条/玻璃贴纸双语言、CanvasToolbar/CanvasSidebar 用克制玻璃面板——大方向见设计草案「视觉与布局」。
- 手机端整体不做（见设计草案「手机端范围」块），本文所有尺寸只考虑桌面端。
