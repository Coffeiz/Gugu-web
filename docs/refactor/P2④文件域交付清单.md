# P2④ 文件域重构 — 交付检查清单

> 覆盖本批全部改动：前端纯函数抽取（①）、optimisticMutation + fetchStorage 修复（②）、
> 后端 keys.py 收敛（③）、contents 域类型化、导航 navPathFor（子步 a）。
> 约定：`[ ]` 未验 / `[x]` 通过 / `[!]` 有问题（旁注现象）。
> 关联：[功能验收清单.md](../product/功能验收清单.md) F 段；[乐观更新盘点.md](文件域乐观更新盘点.md)。

## 提交清单（本批）

| 提交 | 内容 |
|---|---|
| `f34a133` | fmtBytes → utils/fileSize.ts |
| `e28c74e` | resolveFolderIds → utils/folderKeys.ts |
| `0e6bccf` | doneYear/doneMonth/splitName → utils/fileParse.ts |
| `df033e5` | 乐观更新盘点文档 |
| `c15c880` | **fetchStorage 一致性修复**（删/还原/复制后配额即时刷新） |
| `916ee98` | optimisticMutation → utils/，迁 5 处 |
| `2952f40` | 后端 key 纯函数 → services/storage/keys.py |
| `1952372` | contents 域类型化（files:FileMeta[] / folders:FolderCard） |
| `d790bd0` | navPathFor → utils/filesNav.ts（enterFolder 路径规则） |

---

## 一、自动化门禁（提交前必须全绿）— ✅ 已核对 2026-07-11

- [x] 前端 `npm run typecheck` = 0
- [x] 前端 `npm run typecheck:strict` = 0
- [x] 前端 `npx vitest run` 全过（163 passed，含 fileSize/folderKeys/fileParse/optimisticMutation/filesNav）
- [x] 后端 `pytest -q` 全过（199 passed，含 tests/test_storage_keys.py）
- [x] 后端 `python scripts/check_ownership.py` 绿
- [x] 后端 `python scripts/check_confirm_gate.py` 绿

## 二、后端（③ keys.py — 纯移动，存量数据边界）

- [ ] 上传文件（个人/项目/思维/素材四空间）：storage key 路径与迁移前一致，落盘位置不变
- [ ] 同名文件上传：冲突改名 `名(1)` 仍生效（_resolve_conflict）
- [ ] 已有历史文件仍可正常读取/下载/预览（key 逻辑未变，存量不受影响）
- [ ] 回收站还原文件：走 _build_key/_resolve_conflict 的路径正确（trash.py 改了 import）
- [ ] 咕咕（Agent）建/存/移文件：与手动操作行为一致（agent/tools/files.py 改了 import）
- [ ] 红线未动：删除仍进回收站 30 天可还原、get_owned 授权正常、永久删/清空确认门仍在

## 三、前端手动 UI 验收（自动化测不到的交互）

**导航（子步 a：navPathFor 等价替换，重点验「没回归」）**
- [ ] 个人文件 / 项目文件 / 回收站 根入口互相切换正常
- [ ] 项目文件 → 状态组 → 已完成 → 年 → 月 → 项目 逐层进入，面包屑正确
- [ ] 从「月」层进某项目：面包屑保留 已完成/年/月 上下文
- [ ] 文件夹内进子文件夹（个人根 / 项目根 / 嵌套三种起点）路径正确
- [ ] 面包屑点任意段跳回、前进/后退按钮正常（历史栈未动，应无回归）
- [ ] 刷新页面后导航位置恢复（sessionStorage 未动）

**存储用量即时刷新（② fetchStorage）**
- [ ] 删文件 / 删文件夹：顶部配额**立即下降**
- [ ] 回收站还原 / 批量还原：配额**立即回升**
- [ ] 复制粘贴文件：配额**立即上升**
- [ ] 永久删 / 清空回收站：配额不变（符合预期）

**乐观更新 + 失败回滚（② optimisticMutation，5 处）**
- [ ] 删文件（卡片/右键）、拖动移动、剪切粘贴：视图**立即**反映
- [ ] **断网**重试上述操作：失败后**自动回滚**回原状态，界面与后端最终一致

**纯函数 / 类型化不回归（①、contents 类型化）**
- [ ] 多选文件夹后移动/删除/剪切：真作用于所选（resolveFolderIds）
- [ ] 已完成项目按年/月分组显示正确（doneYear/doneMonth）
- [ ] 上传幽灵卡文件名/扩展名拆分正确（splitName）
- [ ] 文件列表/选择/框选/拖拽整体无回归（contents 标类型为 type-only，运行时不变）

## 四、部署

- [ ] 本地改动已 Mutagen 同步到 devserver（前端 HMR 生效；后端改了 .py 需重启服务）
- [ ] 后端服务重启后无启动报错（import 链：files.py / trash.py / agent/tools/files.py / storage/keys.py）
- [ ] devserver 上跑一遍「三」的关键路径确认

## 五、遗留（不在本批，另开刀）

- [ ] 导航子步 b：useFilesNav composable（历史栈+派生，loadContents 注入）
- [ ] 导航子步 c：loadContents 投影（高危）
- [ ] navPathFor 的 yearSeg 缺失防御性修复（本批保留潜伏 bug，等价迁移）
- [ ] P2 第 5 步：P2-b 异常收敛立规则
