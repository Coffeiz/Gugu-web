# PRD：文件存储抽象与生命周期一致性（Web 后端 · 2026-07-11）

> **状态**：**P0（抽象缝）+ P1（目录一致性）+ P2（软删/回收站/乐观并发）+ P3（Agent/REST 统一）已落地**；P3 人工验收待执行，P4（OSS）未做。见下方《验收 Todo》与文末《实现清单》。
> **一句话工单**：把 Web 文件系统从「**路径直拼**」升级成「**领域模型 + 可换存储后端**」——
> 业务代码只经 `File Service` / `StorageBackend`，不再直接拼 `/upload/.../file`。
> **近期真实风险不是迁移成本，是数据完整性**：丢文件 / Agent 删不可恢复 / DB 与盘不一致。
> **前提**：生产未来**几个月仍用本地 `upload/` 目录**；OSS 是几个月后的事。所以现在做「演进层」，不做完整 OSS 迁移。
> 关联：[[gugu-p2-refactor-plan]]、[[gugu-agent-reliability]]、`app/services/storage/`。

---

## ✅ 验收 Todo（P0-P3 文件存储与 Agent 一致性）

> 进度：**P0 全绿 + P1 全绿 + P2 全绿 + P3 自动测试全绿**（backend 429 passed，零回归；前端 typecheck/strict 双绿）。下面分「自动（已过）」与「需你在开发机/staging 手动验收」。勾掉即验收通过。

### A. 自动验收（pytest，已过 ✅）
- [x] KeyStrategy 对拍 `_build_key`（等价、零行为变化）—— `test_key_strategy`
- [x] FolderTree / FileService 文件夹操作 + 领域异常 status —— `test_folder_tree` `test_folders_api`
- [x] 文件写操作走 KeyStrategy：上传/改名移动/复制 + 配额/覆盖/冲突改名/越权 —— `test_file_service` `test_files_api`
- [x] StorageBackend 契约（put/get/delete/rename/exists/copy/stat + 文件夹钩子）—— `test_storage_contract`
- [x] 目录一致性：建空夹上盘、改名清 adr、空夹改名搬目录、**子夹移出保留活父夹**、文件移动保留源夹 —— `test_file_service`
- [x] folder_doctor：报缺失/孤儿、补缺失、确认后清孤儿、忽略结构目录/非空目录、位置不一致文件（misplaced_files）—— `test_folder_doctor`
- [x] 文件夹软删/恢复：物理搬 trash/搬回、目录骨架清理/补回、恢复排除更早独立删除的子孙、conflict 改名 —— `test_file_service` `test_trash_folders`
- [x] 回收站列文件夹（只列顶层）+ 恢复端点 + 过期清理硬删过期文件夹 —— `test_trash_folders`
- [x] 乐观并发：改名/移动版本对不上 409、`soft_delete`/`restore` 顺带失效旧 version —— `test_folder_tree` `test_folders_api`
- [x] 新增正确性收紧：不能往已软删的文件夹里传新文件/挪文件进去 —— `test_file_service`
- [x] 守卫：`check_ownership` / `check_confirm_gate` 绿
- [x] 前端 `typecheck` + `typecheck:strict` 绿（P2.6 改了 6 处调用点 + Folder 类型/缓存）

### B. 手动验收（已完成 ✅）
- [x] **建空文件夹 → 盘上真出现空目录**（治 123）：网页新建一个空文件夹，`ls upload/{uid}/个人文件/` 能看到它。
- [x] **文件夹改名 → 旧目录消失、无 adr 幽灵**：建夹放文件→改名→盘上只剩新名目录，旧名目录已清。
- [x] **子文件夹移出后，原父空文件夹仍在**（反向 123 回归）：parent/child 结构，把 child 移到根，parent 空目录**不能**消失。
- [x] **上传/覆盖/改名/移动/复制** 走一遍网页流：文件落在正确路径、覆盖保 id、同名自动加后缀、跨空间移动生效。
- [x] **存储对账后台面板**：后台侧栏「运维 → 存储对账」→ 文件对账 + 目录对账两段 → 扫描看报告 → 确认后修复。
- [x] **存量修复**：对生产/staging 现有用户完成扫描与必要修复。
- [x] **P2.1 Alembic 迁移（devserver）**：2026-07-15 确认 devserver 已处于 `20260715000001 (head)`，存量数据正常。
- [x] **P2.1 Alembic 迁移（生产）**：已确认生产数据库迁移到目标 head。
- [x] **文件夹删除→回收站→恢复/永久删除**：已完成网页流验收，文件夹整体恢复、子文件层级与永久删除均符合预期。
- [x] **改名/移动乐观锁**：已验证并发修改时返回「文件夹已被修改，请刷新后重试」，不会静默覆盖。

### C. P3 手动验收（待执行）
- [ ] 网页端创建个人文件夹，确认数据库记录和 `upload` 下实体目录同时出现。
- [ ] 网页端创建项目文件夹，确认项目目录和数据库归属一致。
- [ ] 咕咕创建、重命名、移动个人文件夹，确认网页端刷新后名称、层级和文件路径一致。
- [ ] 咕咕跨项目移动文件夹，确认目标项目下可见，原项目下消失，目录内文件仍可打开。
- [ ] 删除包含多级子文件夹和文件的目录，确认回收站只显示顶层文件夹，不重复列出子文件。
- [ ] 分别通过网页端和咕咕恢复文件夹，确认目录树、文件状态、文件内容和物理路径都恢复。
- [ ] 恢复时制造同名文件夹/文件冲突，确认自动改名且不覆盖现有内容。
- [ ] 尝试恢复已删除文件夹内的单个子文件，确认被拒绝并提示先恢复文件夹。
- [ ] 确认普通删除不再弹浏览器确认框，清空回收站仍保留确认门。
- [ ] 用非当前用户的文件夹或文件 ID 调用咕咕工具，确认不会返回资源存在性或执行越权操作。
- [ ] 检查咕咕工具失败提示不包含原始异常、路径细节或其他用户信息。

### D. 明确未做（不在本轮验收范围）
- Agent 工具走 FileService（P3）、OSS lazy 迁移作业（P4）。
- `presign`/`confirm`（OSS 直传端点）仍裸调 `_build_key`，随 P4 收。
- 文件夹回收站仅允许对顶层文件夹执行单项永久删除；操作会一并清理其整棵已删子树及内部文件，子文件不单列、不支持单独删除。

---

## 0. 范围（写死，防越界）

**现在做（Web 1.0，Local 还要用几个月）**：
- `StorageBackend` 抽象——**把 `upload/` 当成一种后端实现，而不是整个文件系统**。
- `KeyStrategy` 接口——**现在保留 `PathMirrorStrategy`（存量数据零迁移）**，opaque 留作未来可换实现。
- 文件夹**生命周期一致性**（DB 状态 ↔ 磁盘状态）：修当前的丢失/幽灵/不可恢复。
- `File Service`——**REST 与 Agent 共用同一套文件语义**。
- 文件夹**软删 + 整体恢复**；埋 `storage_backend / version / updated_at / deleted_at` 字段（含 `storage_backend`，为快速切 OSS 留好通道）。

**OSS 到来时才做**（几个月后，或需求突增时；字段已就位、可快速切）：
- `OSSStorageBackend`（**阿里云 OSS**，已定）；**OSS 切 `OpaqueStrategy`**（已定，`{uid}/blobs/{uuid}.{ext}`）。
- lazy 双后端：新文件→OSS/opaque、旧文件留本地慢慢迁——**迁移既换后端也换 key 方案**（path-mirror→opaque，逐个 re-key）。

**更以后再决定**：
- 桌面端 / 双向同步 / 冲突 / operation log。

> 原则：**现在把「文件操作入口」收紧就够了；不为未来把生产文件系统提前重写。**
> **别为未来桌面端把后端写成同步系统；但今天的后端不能挡住未来。**

## 1. 现状诊断（当前要修的 root cause）

`Folder` 是纯 DB 行（无 storage_key/物理路径，`app/models/__init__.py:156`）；物理存在只靠文件 key 内嵌路径 + 业务代码直拼 `/upload`。当前**生产在跑**的三个数据完整性问题：
- **空文件夹不上盘**：建文件夹从不 `mkdir`，只在写文件时隐式建（`storage/__init__.py:113`）。
- **rename 留幽灵目录**：`rename_file`（`:130`）建新父但**不删空旧目录**；`delete`（`:119`）只清一层 → 移动/改名后残留（如两个 `adr`）。
- **删文件夹无法恢复**：`delete_folder` 硬删文件夹行（`db.delete`）。

**根因**：文件夹缺物理生命周期 + 业务层直接拼路径、无统一入口。**这三个是当前线上真实风险**，与 OSS 无关，先修。

## 2. 目标抽象（现在就落）

```
                REST API        Agent 工具
                    \             /
                     File Service          ← 唯一文件语义入口（REST 与 Agent 共用）
                    /            \
             FolderTree      StorageBackend
             (领域层)          (字节层)
                 |                |
             SQLAlchemy    LocalStorageBackend → /upload   （未来加 OSSStorageBackend）
                                  |
                             KeyStrategy = PathMirror（现在）  （未来可换 Opaque）
```
> **当前实现** = PostgreSQL + Local(`upload/`) + PathMirror；**未来** = PostgreSQL + OSS + Opaque。
> 换的是最底下两层（StorageBackend / KeyStrategy），**上层 REST / Agent / FileService / FolderTree 不变**——这就是这套抽象的全部意义。

- **`File Service`**：建/移/删/改名/软删/列举的**唯一实现**，REST 与 Agent 都调它 → 天然一致。
- **`FolderTree`**：文件夹树的 Web 后端领域层（`getFolder/getChildren/createFolder/moveFolder/deleteFolder/resolveLogicalPath`），当前实现 = SQLAlchemy。**保持为接口**（未来桌面端可另给实现，白捡；非当前目标）。
- **`StorageBackend`**：只管字节。现在只有 `LocalStorageBackend`（包 `upload/`）。**未来 OSS 是加一个实现,不动业务层。**
- **`KeyStrategy`**：现在 `PathMirrorStrategy`（`{uid}/项目文件/.../file`，存量数据不变、可挂盘浏览）。**OSS 到来时切 `OpaqueStrategy`（已定）**；Local 保持 path-mirror。

> **YAGNI**：现在只实现 `Local` + `PathMirror` 一条。`OSSStorageBackend`（阿里云）+ `OpaqueStrategy` 只留接口、不写实现，等 OSS 到来。
> **接口前向兼容**：`KeyStrategy.build_key(ctx)` 的 `KeyContext` 只放存储字段（`user_id/file_id/name/ext/logical_path`），PathMirror 取 `logical_path`、Opaque 取 `file_id`，**加 Opaque 不改签名**。业务命名（项目/年月/文件夹→路径）拆到 `compose_logical_path` 纯函数，不进存储层。**冻结签名见附三。**

## 2.5 目录结构：重构前 → 重构后（P0–P3）

**前（现在）**：`__init__.py` 抽象+实现混装；业务层各处直接拼 key、REST 与 Agent 各写一套 folder 逻辑。
```
app/services/storage/
├── __init__.py   (269)   StorageBackend 抽象 + Local/OSS backend + get_storage()  ← 混装
├── keys.py       (61)    _build_key / _resolve_conflict / _safe_name
└── folders.py    (128)   resolve_folder_path / relocate（旧 WIP，DB 耦合）
app/api/v1/
├── folders.py    (322)   文件夹 REST —— 直接调 keys + DB
├── files.py      (1256)  文件 REST —— 直接拼 key + 存储
└── trash.py      (197)   回收站 REST
agent/tools/
└── files.py      (1432)  咕咕文件工具 —— 另写一套 folder 逻辑（易漂）
```

**后（P0–P3 完成）**：单一入口 `FileService`，接口后面可换后端/策略；REST 与 Agent 共用。
```
app/services/storage/
├── __init__.py         re-export（保持导入路径兼容）
├── base.py             StorageBackend 抽象 + KeyStrategy 协议 + FolderTree 接口      [P0]
├── factory.py          get_storage / get_file_service / get_folder_tree / …          [P0.4]
├── file_service/     ★ 对外一个 FileService 门面，内部按职责拆（防长成新 god file）  [P0.3]
│   ├── __init__.py     FileService（薄门面，聚合下面四块）
│   ├── files.py        文件 CRUD
│   ├── folders.py      文件夹操作
│   ├── move.py         移动 / 改名
│   ├── trash.py        软删 / 恢复
│   └── helpers.py      内部共用（路径解析/校验等，非公开）
├── folder_tree.py      FolderTree 接口 + SqlAlchemyFolderTree（吸收旧 folders.py）    [P0.2]
├── key_strategy.py     KeyStrategy 协议 + PathMirrorStrategy（包 keys.py）            [P0.1]
├── keys.py             _build_key / _resolve_conflict（保留，被策略包）
├── folder_doctor.py    对账工具：补缺失目录 / 报告孤儿                                [P1.5]
└── backends/
    ├── local.py        LocalStorageBackend + 目录原语（ensure_dir/清空祖先/move）    [P1]
    └── oss.py          阿里云 OSS backend（现有实现挪入，未启用；opaque）         (P4)
app/api/v1/
├── folders.py          瘦身：端点 delegate 到 FileService                            [P0.5]
├── files.py            瘦身：delegate 到 FileService                                 [P0.5]
└── trash.py            回收站：列文件夹 + 整体恢复                                    [P2]
agent/tools/
└── files.py            瘦身：folder 操作 delegate 到 FileService（咕咕/网页一致）     [P3]
```

**连线（谁拆成谁 / 谁收口到谁）**：
```
storage/__init__.py ─┬─→ base.py            抽象接口（StorageBackend/KeyStrategy/FolderTree）
 （混装，拆开）        ├─→ backends/local.py  Local 实现  ＋P1 目录原语
                     ├─→ backends/oss.py    OSS 实现（原样挪入，未启用）
                     └─→ factory.py         装配

storage/keys.py ─────→ keys.py（保留）  ◄──包──  key_strategy.py : PathMirrorStrategy

storage/folders.py ──┬─→ folder_tree.py     DB 树逻辑（resolve_path / 增删改查）
 （旧 WIP，拆开）      └─→ file_service.py     move / 物理归位

REST  folders.py ────┐
REST  files.py ──────┼── delegate ──→  ★ file_service/  （薄门面，唯一入口）
Agent tools/files.py ┘                          │
                                    用 folder_tree + storage backend + key_strategy

＋ 新增：file_service.py · folder_tree.py · key_strategy.py · folder_doctor.py
```

> **本质变化**：`__init__.py` 的「抽象+实现混装」拆成「`base` 接口 / `backends` 实现 / `factory` 装配」；
> 「拼 key + folder DB 操作」从 REST 与 Agent 各处收口到 **`FileService` + `FolderTree` + `KeyStrategy`** 三件。
> **OSS 到来（P4）**：启用 `backends/oss.py`（阿里云）+ 切 `OpaqueStrategy` + 加 `migrate.py`（方向无关作业，迁移时 path-mirror→opaque re-key）。**未来桌面端**：把 local 逻辑再拆出可移植的 `local/tree.py`（见附一）。

## 3. 文件夹生命周期一致性（当前主线）

**要求**：`Folder DB 状态` 与 `磁盘状态`始终一致。因为 Local 还要用几个月，这是**当前主要交付、不是过渡投入**：
- 建文件夹 → `mkdir` 真目录（修「空文件夹不上盘」）。（Local 空目录用真 `mkdir`，不用 `.keep`，避免污染用户可见文件。）
- 纯改名 → `rename_dir`（整目录 `mv`，原子、不留孤儿）。
- 移动 → 目录/文件同步，**逐级清空的旧祖先目录**。
- 删文件夹 → 物理搬子树进 trash 前缀（配合 §5 软删）。
- 修 `rename_file`/`delete`：清空后**逐级 rmdir** 旧祖先。
- **一次性清幽灵**：对账工具 diff DB 树 vs 磁盘，补缺失目录、报告孤儿（非空目录人工确认后清），治现存 `adr`/123。**由管理员在后台入口触发（扫描→报告→确认清理）+ 低频兜底，不做小时级常驻扫描。**

> 未来若切 opaque（文件夹变纯 DB 行、无物理目录），这套生命周期会大幅简化甚至消失——但那是未来；**现在就是要把 Local 修对**。

## 4. Agent / REST 一致（当前价值最高之一）

Agent 的 `_create_folder`/`_delete_folder`/移动等，**全走同一个 `File Service`**（现在各写一套：REST 走 `folders.py`，Agent 走 `tools/files.py`，语义容易漂）。一致靠共用实现 + 对称测试（沿用 projectStages / agent 工具模式，不合并成单一代码源）。

## 5. 软删：回收站 + Agent 执行安全

`deleted_at` 软删 + 整体恢复：
- **回收站**：文件夹可整体恢复（修 §1 第三个 bug）。
- **Agent 执行安全层**：咕咕删目录 = 可撤销 tombstone，是「安全操作真实世界」的信任底座（与 P2-b、confirm-gate 同一条可靠性主线，见 [[gugu-agent-reliability]]）。
> （将来还能当同步删除墓碑——白捡的未来价值，现在不为它设计。）

## 6. 现在埋的字段（低成本，不实现未来系统）

Alembic 加列、nullable/带默认、不回填即安全：
```
File:   storage_backend(默认 'local')  version  updated_at  deleted_at
Folder: version  updated_at  deleted_at
```
- `storage_backend`：**现在就加**（默认 `'local'`，所有现存+新文件都是 local，零行为变化）。目的是**给「需求突增直接切 OSS」留好快速通道**——真要切时不必再卡一次 schema 迁移，直接上 `OSSStorageBackend` + 路由。加列 ≠ 建系统。
- `version`/`updated_at`：当前用于乐观并发（延伸现有 409 并发锁）；未来同步冲突裁决白捡。
- `deleted_at`：软删（§5）。
> **只加字段，不建** SyncEngine / ConflictResolver / OperationLog。`OSSStorageBackend` 实现 + lazy 迁移逻辑仍等 OSS 到来再写（字段就位 ≠ 系统建好）。

## 7. 分阶段路线（Web 后端，按数据完整性优先）

**现在做**：
| 阶段 | 内容 | 驱动 |
|---|---|---|
| **P0** | `StorageBackend`/`KeyStrategy(PathMirror)`/`FolderTree` 接口 + `File Service` + `factory`（**零行为变化**，把入口收紧） | 可换后端地基 |
| **P1** | 文件夹**生命周期一致性**修复（mkdir / rename 清孤儿 / 一次性清幽灵） | **数据完整性（线上风险）** |
| **P2** | 文件夹**软删 + 回收站整体恢复** + 埋 `storage_backend/version/updated_at/deleted_at` | Agent 安全 + 数据可恢复 + 快速切 OSS 就绪 |
| **P3** | **Agent / REST 统一到 `File Service`** + 对称测试 | 咕咕一致 |

**OSS 到来时**（几个月后）：
| P4 | `OSSStorageBackend`（**阿里云**）+ 切 `OpaqueStrategy` + **方向无关的 lazy 迁移作业**（迁移时 path-mirror→opaque re-key） `migrate(from_backend, to_backend)`：`local↔oss` 双向对称（含 **OSS→本地逃生门**）；由**管理员按钮触发**的受控后台作业——新传走目标后端、旧文件读时迁+低频冷扫，**非一键同步搬完**（详见附二） |

**更以后**（现在不做、只留接口）：opaque key 决策；桌面端；双向同步 / op-log。

> 纪律：每阶段独立提交；先补对称/单元测试再改；生产相关先 dry-run + staging + 灰度。

## 8. 生产安全红线

1. **存量 `storage_key` 不动**（现在保留 path-mirror，不迁移；OSS 到来再逐个 lazy 迁）。
2. 新列走 Alembic nullable、不回填即安全。
3. 对账器只补目录 / 报告孤儿，非空目录先报告人工确认，绝不自动删。
4. 全程 `get_owned` 授权 + 删除 30 天可还原（P2④ 红线）。
5. Local 生命周期修复也先 staging 验证再上生产（涉及真实文件的 mkdir/rmdir/搬迁）。

## 9. 待拍板

- ~~对账工具：一次性脚本 vs 后台管理入口~~ → **已定：后台管理入口**（admin API + 面板页）。
- ~~接口最终签名~~ → **已冻结，见附三**（含 `FileService(db)` 构造、领域异常放 `app/core/errors.py`、StorageBackend 补 stat/copy/文件夹钩子、KeyContext 瘦身）。
- **§9 全部已定** —— PRD 可进入 P0 实现。
- ~~opaque 是否切、OSS 厂商~~ → **已定：OSS = 阿里云 + 切 `OpaqueStrategy`**（Local 保持 path-mirror；迁移 re-key）。

## 附：与未来的关系（仅「不阻碍」约束，非本 PRD 目标）

- `StorageBackend` 接口 → 未来 OSS 是加实现、换 factory，业务层不动。
- `KeyStrategy` 接口 → 未来若切 opaque 是换策略，不重写业务。
- `FolderTree` 接口 → 未来桌面端可给 `LocalFolderTree`(SQLite) 实现；领域规则靠《行为规格》+ 对称测试一致（不追代码复用）。
- storage 模块把 Local 逻辑与（未来）OSS SDK 分目录，避免桌面端打包拖依赖。
> 这些是**结构上顺手留的口子**，本 PRD **不实现**任何 OSS / 桌面 / 同步逻辑。

## 附二：迁移 vs 同步的边界（防混淆）

「OSS→本地」和「同步到客户端」共用一块地基，但**不是同一个功能**：

| | OSS→本地迁移（运维） | 客户端双向同步（桌面端·围栏外） |
|---|---|---|
| 在哪 | 服务端 | 每台用户设备 |
| 方向 | 单向（换权威后端） | 双向（两副本收敛） |
| 复杂度 | 低——切 OSS 按钮的镜像，`migrate(oss, local)` | 高——分布式，需冲突解决/墓碑/version |
| 归属 | 本 PRD 的 P4（方向无关作业顺带出） | **围栏外的未来大件（SyncEngine）** |

**共用地基**：两者都要「把 DB 树 + 文件**物化成真实本地目录**」这个原语 = `LocalStorageBackend` + `local/tree.py`。
- OSS→本地：服务端「读 OSS 后端 → 写 Local 后端」。
- 客户端同步：客户端「从 API 拉树 → 写本机 FS」，**同一套物化逻辑，上面再叠双向同步**。

> 所以现在（P0/P1）把服务端 Local backend + 目录物化做扎实，一箭双雕：治 today 的同步 bug + 铺客户端本地镜像引擎的地基。但**「客户端双向同步」本身不在本 PRD**——它是围栏外的分布式大件。

## 附三：P0 接口签名（冻结版）

> 评审冻结（Claude 草案 + GPT 精修）。P0 按此实现，签名不再讨论。

**KeyStrategy**（瘦身：只放存储需要的数据，不放 `project_year/mind_map_title` 等业务字段）
```python
@dataclass
class KeyContext:
    user_id: UUID
    file_id: int | None       # Opaque 用；PathMirror 忽略（可 None）
    name: str
    ext: str
    logical_path: str = ""    # 已由上层解析好的「可浏览逻辑路径」，如 项目文件/2026/07/P #3/设计

@dataclass
class ResolvedKey:            # 冲突改名后 key 与 name 都变，两者都要落库（storage_key + display_name）
    key: str
    name: str

class KeyStrategy(Protocol):
    def build_key(self, ctx: KeyContext) -> str: ...
    async def resolve_conflict(self, storage, base_key, display_name, ext) -> ResolvedKey: ...
    @property
    def move_semantics(self) -> Literal["relocate", "db-only"]: ...
```
- **`compose_logical_path(space, project, folder_path, …) -> str`**：业务命名（可浏览树布局）从 `_build_key` 拆出、放近 FileService 的纯函数；KeyStrategy 只吃结果 `logical_path`。
- `PathMirrorStrategy`：`{uid}/{logical_path}/{safe_name(name)}.{ext}`；`move_semantics="relocate"`。
- `OpaqueStrategy`(P4)：`{uid}/blobs/{file_id}.{ext}`（忽略 logical_path）；`move_semantics="db-only"`。**加它不改签名。**

**FolderTree**（只管元数据 DB，`get_owned` 语义；**绝不含 `os.rename`/mkdir**）
```python
class FolderTree(Protocol):
    async def get(user_id, folder_id) -> Folder | None                       # 未拥有→None
    async def get_children(user_id, *, project_id, parent_id) -> list[Folder]  # 根传 None（渲染 IS NULL）
    async def resolve_folder_path(user_id, folder_id) -> str | None          # 文件夹名链（如 设计/评审）；坏链/越权→None
    async def descendants(user_id, folder_id) -> list[int]                   # 含 root
    async def create(user_id, *, name, parent_id, project_id) -> Folder      # raise NotFound(项目)/Conflict(重名)
    async def rename(user_id, folder_id, new_name) -> Folder                 # raise NotFound
    async def move(user_id, folder_id, new_parent_id) -> Folder              # raise NotFound/Invalid（循环/跨空间）
    async def soft_delete(user_id, folder_id) -> None             # P2
    async def restore(user_id, folder_id) -> None                 # P2
# 实现微调（acb3961）：resolve_logical_path→resolve_folder_path（返文件夹名链，喂 compose_logical_path）；
# get_children 取 project_id+parent_id；mutation 校验失败抛领域异常、返 Folder（非 Optional）；只 flush 不 commit。
```

**StorageBackend**（现有 + 新增；**物理动作由 backend 自决，FileService 不 `if local`**）
```python
class StorageBackend(Protocol):
    async def put/get/delete/exists(...)          # 现有
    async def copy(src, dst) -> None              # 新增（OSS rename=copy+delete 的基元）
    async def rename_file(old, new) -> None       # 无原生 rename 的后端默认 copy+delete
    async def stat(key) -> StorageObjectInfo      # 新增：size/checksum/mtime（迁移校验用）
    async def list_prefix(prefix) -> list[str]; def presign(...)
    # 文件夹生命周期钩子（Local 真 mkdir/mv/rm；OSS no-op）：
    async def ensure_folder(logical_path) -> None
    async def move_folder(old_path, new_path) -> None
    async def remove_folder(logical_path) -> None
    async def remove_empty_ancestors(key) -> None   # Local 清孤儿
```

**FileService**（薄门面，`FileService(db)` 构造；抛**领域异常**不抛 HTTPException）
```python
class FileService:                # get_file_service(db) 一处装配：FolderTree(db)+get_storage()+get_key_strategy()
    # 文件：create_file / read_file / rename_file / move_file / delete_file(软删) / list_files
    # 文件夹：create_folder / rename_folder / move_folder / delete_folder(软删+trash) / restore_folder
```
- `create_folder` = `FolderTree.create()` + `storage.ensure_folder(path)`（无 `if local`，backend 自决）。
- `move_folder` = `FolderTree.move()` + `storage.move_folder(old,new)`（认知 vs 物理分离）。
- **领域异常放 `app/core/errors.py`**：`NotFound / NotOwned / Conflict` = P2-b 的 `ExpectedError` 子类（**两 PRD 合流，不建两套**）。REST 映射 HTTPException、Agent 映射 `{"error"}`。
- **返回 DTO/schema，不泄漏 ORM**：`FolderTree`（内部）返 ORM，`FileService`（公开）在边界映射成 `FileDTO/FolderDTO`（或复用现有 `FileResponse/FolderResponse` schema）→ REST/Agent/桌面端都不绑 SQLAlchemy Model。
- **create 顺序**：PathMirror（现在）key 不依赖 id → 先算 key 再 insert；Opaque（P4）需 id → insert → build_key → upload → 回填。**P4 不止把 `storage_key` 改 nullable，还加 `storage_status`（pending/uploaded/failed）**——「insert 成功、upload 失败」是必然，NULL key 无法区分「上传中/失败/垃圾」，有状态才能清理与重试。（P0 不需要。）

---

## 实现清单（TODO · 展开自 §7 路线，未动代码）

> **通用纪律**：① 每 task 独立提交；② 先补测试再改；③ 涉及真实文件的 mkdir/rmdir/搬迁**先 staging 验证**；
> ④ 零行为变化的重构靠现有 REST 测试 + 对称测试兜底；⑤ 守红线：`get_owned` 授权、删除 30 天可还原、
> **不动存量 `storage_key`**、`check_ownership.py`/`check_confirm_gate.py` 保持绿。

### 前置（P0 起手时先定）
- [ ] `FileService` / `FolderTree` / `KeyStrategy` 接口最终签名（评审 30 分钟拍板）。
- [x] 对账工具形态：**后台管理入口**（管理员在后台触发扫描 → 看差异报告 → 人工确认清理孤儿），非 CLI。需 admin API 端点 + 后台面板页。

---

### P0 — 抽象缝（零行为变化，最安全，先做）

目标：把现在散落在 `folders.py`/`files.py`/`agent/tools/files.py` 的「拼 key + 文件夹 DB 操作」收口到接口后面，**行为一字不变**。

- [x] **P0.1 `KeyStrategy` + `PathMirrorStrategy` + `compose_logical_path`**（签名见附三）✅ `107e52e`
  - `KeyContext` 瘦身（user_id/file_id/name/ext/logical_path，不含业务字段）。
  - `compose_logical_path(...)` = 从 `_build_key` 拆出的**业务命名纯函数**（space/项目/年月/文件夹 → 逻辑路径）。
  - `PathMirrorStrategy.build_key` = `{uid}/{logical_path}/{name}.{ext}`（物理拼装，含 `resolve_conflict`）。
  - 测试：现有 `test_storage_keys.py` 向量拆成「compose 段 + PathMirror 拼装段」两组，合起来与现 `_build_key` 逐字一致（对拍）。
- [x] **P0.2 `FolderTree` 接口 + `SqlAlchemyFolderTree`** ✅ `acb3961`
  - 新 `storage/folder_tree.py`：接口 `get_folder / get_children / create_folder / rename_folder / move_folder / delete_folder / resolve_path`。
  - `SqlAlchemyFolderTree` = 把 `folders.py` 的 DB 查询 + `storage/folders.py` 的 `resolve_folder_path` 抽进来（DB 耦合逻辑集中一处）。
  - 测试：测试库/内存，覆盖 create/move/rename/delete/resolve_path + 归属校验（`get_owned` 语义）。
  - 验收：与现 `folders.py` 行为等价；`check_ownership.py` 绿。
- [x] **P0.3 `FileService`（薄门面，内部按职责拆）** ✅ 文件夹切片 `e80f25e`；文件写操作 P0.3b
  - 新 `storage/file_service/`：**对外只暴露一个 `FileService`**；内部拆 `files.py`(文件写：create/update/copy) / `folders.py`(文件夹)，**避免长成新的 god file（新 `storage/__init__.py`）**。
  - **P0.3b**：`create_file / update_file(改名移动) / copy_file` 走 KeyStrategy（`compose_logical_path` + `build_key` + `resolve_conflict`），逐字复刻 `files.py` 的 upload/PATCH/copy（key/配额/覆盖/冲突改名/跨空间归属）；`FileResult(file, project, folder_name, was_overwrite)` 供端点 shape 响应。软删（delete/回收站）归 P2、纯查询（list/tree）留端点——与文件夹切片对称（只搬 key 承载的写操作）。
  - `FileService(db)` 构造（factory `get_file_service(db)`）；抛领域异常 `NotFound/NotOwned/Conflict`（放 `app/core/errors.py`，= P2-b `ExpectedError` 子类），**不抛 HTTPException**。
  - 物理动作由 backend 自决（`storage.ensure_folder/move_folder`），FileService **不 `if local`**。
  - 验收：单测覆盖各操作；不含 FastAPI 依赖（REST 与 Agent 共用）；单文件不超 ~300 行。
- [x] **P0.4 factory 装配** ✅
  - `storage/factory.py`：`get_file_service() / get_folder_tree() / get_key_strategy()` 按配置装配（现在只 `Local + PathMirror`，YAGNI）。
  - 验收：一处装配、下游注入。
- [x] **P0.5 REST 端点 delegate 到 `FileService`** ✅ 文件夹端点 `5cc1493`；文件端点（upload/PATCH/copy）P0.3b
  - `folders.py`/`files.py` 各写端点改为调 `FileService`（逐端点等价替换）；端点保留传输面（`UploadFile` 读、413 闸、图片尺寸解、缩略图调度/清理、事务与事件、响应 shape）。
  - 覆盖上传**不发 files 事件**（复刻原行为）；413 单文件上限留端点（请求体传输约束）。
  - 待办：`presign`/`confirm`（OSS 直传，仍裸调 `_build_key`）暂留——OSS 到来时随 P4 一并收；软删端点 delete/batch-delete 归 P2。
  - 验收：**所有现有文件/文件夹 REST 测试通过**；无行为变化；`test_files_api` + `test_file_service`(P0.3b) 覆盖 upload/改名/复制 + 领域异常。
- [x] **P0.6 `StorageBackend` 契约测试**（共享测试向量，任何 backend 必过）✅
  - `tests/storage/test_storage_contract.py`：对一个 backend 跑 put/get/delete/rename/exists（P1.1 的 copy/stat/文件夹钩子加入同一套）。
  - 现在 `LocalStorageBackend` 跑；**未来 `OSSStorageBackend` 跑同一套**——迁移信心的地基。

---

### P1 — 文件夹生命周期一致性（数据完整性，治 today 的 123/adr）

- [x] **P1.1 `StorageBackend` 文件夹生命周期钩子 + stat/copy**（签名见附三）✅ `e7e6124`
  - 加 `ensure_folder/move_folder/remove_folder/remove_empty_ancestors` + `stat/copy`；`LocalStorageBackend` 真 mkdir/mv/rm，对象存储默认 no-op。契约测试 +6。
- [x] **P1.2 建文件夹物化目录**：`FolderOps.create` 调 `ensure_folder` → 空文件夹真 `mkdir` 上盘 ✅ `e7e6124`
- [~] **P1.3 修 rename/delete 留孤儿** ⚠️ **设计修正**：不按文件清祖先。空文件夹须持久（P1.2），文件移出后源文件夹仍存活——按文件清祖先会**误删活着的空文件夹**（重现反向 123）。孤儿只在**文件夹级**清（见 P1.4）+ 对账工具兜底。`e7e6124`
- [x] **P1.4 文件夹改名/移动走目录级**：搬子树文件后对账物理目录——物化新位置整棵子树（空夹也补）+ `remove_folder` 清旧位置搬空骨架（只删目标子树、不清祖先，避免误删活父夹）✅ `e7e6124`。集成测试锁定：改名清 adr、空夹改名搬目录、子夹移出保留活父夹、文件移动保留源夹。
- [x] **P1.5 对账工具（folder doctor）+ 后台入口** ✅ `folder_doctor.py`（scan/repair）+ `GET/POST /admin/folder-doctor/{scan,repair}`（挂 require_admin）+ 前端面板 `views/Admin/FolderDoctor`（侧栏「目录对账」：扫描→报告→勾选确认→执行修复）。缺失目录自动补、孤儿空目录报告+确认（`remove_orphans`）后清、非空绝不纳入；孤儿判定用「父目录合法性」（containers∪expected），结构目录天然不误判；**repair 服务端重扫、不接受外部传入待删路径**。测试 +9、typecheck/strict 绿。🔧 实机 UI 待 staging 验。

---

### P2 — 软删 + 回收站 + 字段（Agent 安全 + 可恢复 + 快速切 OSS 就绪）

- [x] **P2.1 Alembic 加列** ✅ `20260714000001_add_storage_p2_fields.py`：`File` 加 `storage_backend`(默认 `'local'`)/`version`；`Folder` 加 `version`/`updated_at`/`deleted_at`。全部 `NOT NULL + server_default`（同 `20260622000002` 给 Project/CalendarEvent 加 version 的先例）——metadata-only 变更，不锁表不回填。2026-07-15 已确认 devserver 处于 `20260715000001 (head)`，存量数据正常；生产部署前仍需确认 `alembic current` 与目标 head 一致。
- [x] **P2.2 文件夹软删** ✅ `delete_folder`（REST + `FileService.delete_folder`）改软删：`FolderTree.soft_delete` 批量打同一时间戳（根+存活子孙）+ `FolderOps.delete` 把当时存活的文件同批软删并搬物理 trash（复用抽出的 `app/services/storage/trash.py`）+ 目录骨架清理（非空保守跳过，仅 path-mirror 下做）。`list_folders`/`list_all_folders`/`download_folder` 同步补 `deleted_at IS NULL` 过滤（否则软删的文件夹还会在文件浏览器里可见）。
- [x] **P2.3 回收站列文件夹** ✅ `GET /trash/folders`：顶层已删文件夹（自身已删 且 父未删/无父）为可恢复单元，`TrashFolderResponse` 带 `deleted_at`。
- [x] **P2.4a 回收站层级语义收紧** ✅ 2026-07-15：回收站仅列独立文件与顶层已删文件夹；文件夹内子文件不单列、不允许单独恢复或删除，必须经文件夹整体操作。前端接入文件夹恢复与单项永久删除；清空回收站同样会永久清理顶层已删文件夹及其子文件。`test_trash_folders` 覆盖隐藏/拒绝恢复/单项删除/清空路径。
- [x] **P2.4 整体恢复** ✅ `POST /trash/folders/{fid}/restore` → `FileService.restore_folder`：`FolderTree.restore` 只恢复与根**同一时间戳**的节点（更早独立删除的子孙不随之恢复，见代码顶注的已知范围限定）+ 物化目录骨架 + 同批文件物理搬回（冲突自动加后缀，不覆盖已有文件）。
- [x] **P2.4b 存储后端回收站边界** ✅ 2026-07-15：回收站动作改走 `StorageBackend.move_to_trash/restore_from_trash`；Local 实际搬移 `upload` 下对象，保留用户目录及子目录、去掉 `个人文件/项目文件/思维` 等空间前缀，并以可读的 `display_name.ext` 存入 `trash/`（同目录物理重名自动加 `(n)`，不改数据库显示名），OSS 保留原 key、仅依赖 `deleted_at` 做逻辑回收，恢复时不会把原对象误判为同名冲突。新增 Local 契约测试；历史随机文件名不做破坏性批量改名。
- [x] **P2.4c 旧回收站迁移** ✅ 2026-07-15：Admin「存储对账」新增扫描/确认迁移入口，把旧版 `trash/{file_id}/...` 迁移到新的目录保留结构；目标冲突不覆盖，缺失对象和非旧结构只报告。
- [x] **P2.5 过期清理** ✅ `cleanup_expired` 扩展：顶层过期文件夹（`deleted_at` 超 30 天）硬删 Folder 行（ORM cascade 连带子树），子树内文件已由既有文件清理逻辑按各自 `deleted_at` 清掉（同批软删共用同一时间戳，自然同批过期）；目录骨架尽力而为收尾重试。
- [x] **P2.6 version 接乐观并发** ✅ `FolderRename`/`FolderMove` 现在必传 `version`；`FolderTree.rename/move` 走原子 `UPDATE ... WHERE version=client_version`，对不上抛 `Conflict`（409，同 Project 的既有模式）；`soft_delete`/`restore` 也各自把 `version` +1（防止并发的改名/移动请求在文件夹已被删除/恢复之后仍凭旧 version 静默成功）。**前端同步改**：`foldersApi.rename/move` 加 `version` 参数，`stores/filesCache.ts` 的 Folder 缓存跟踪 `version` 并在每次改名/移动成功后回写；6 处调用点（`Files/index.vue` 改名+2 处移动、`ProjectModal.vue` 改名+2 处移动）全部同步，`typecheck`/`typecheck:strict` 双绿。
  - **意外发现的正确性收紧**：加了乐观并发之余，顺带堵上一个 P2 才暴露的缺口——`FileOps.create_file/update_file`（及 `presign_upload`）新增「目标文件夹须存活」校验，之前软删一个文件夹后仍能往里传新文件/挪文件进去（因为 `resolve_folder_path` 本身不认 `deleted_at`，`folder_dir_key` 等内部用途需要这个不认——两者用同一个底层函数但目的冲突，改成在调用点加一道判断，不改共享函数本身）。
  - **后续收紧（2026-07-15）**：回收站仅展示独立文件与顶层已删文件夹；归属已删文件夹的子文件不允许单独恢复，必须先恢复文件夹，避免恢复后指向已删目录。

---

### P3 — Agent / REST 统一（咕咕一致）

- [x] **P3.1 Agent 工具走 `FileService`**：文件夹创建、删除、重命名与移动均已统一委托 `FileService`，保留原有 Agent 返回结构和确认门边界。
- [x] **P3.1a 扩展 `FileService.move_folder`**：支持目标项目、目标父文件夹和跨项目移动；统一校验目标项目、循环引用、已删除目标与版本冲突。
- [x] **P3.1b 迁移 Agent 移动路径**：`_move_folder`、`move_items` 的文件夹分支统一委托 `FileService`，保留原有批量结果回显和失败项结构。
- [x] **P3.1c 物理路径回归**：已覆盖同项目改父级、跨项目移动、嵌套子树、空文件夹、冲突文件名、文件路径重建和目录骨架同步。
- [x] **P3.2 对称测试**：Agent 已新增 `restore_folder`，与 REST 一样复用 `FileService.restore_folder`；已覆盖 Agent 文件/文件夹恢复、目录树、文件状态和物理路径回归。
- [x] **P3.3 清重复**：Agent 文件夹业务逻辑已收口到 `FileService`，仅保留参数适配、结果回显和 Agent 专属错误文案；共享存储 helper 未重复复制。
  - 验收：咕咕文件夹操作与网页逐字一致；对称测试全绿；`check_ownership.py`/`check_confirm_gate.py` 绿。

---

### 建议起手顺序

**P0.1（`KeyStrategy` + `PathMirrorStrategy`）** 是最小、最安全的第一刀：纯抽提、有现成测试向量对拍、零行为变化、零生产风险。做完再 P0.2→P0.5 把缝铺满，然后 P1（数据完整性，价值最高）。

> P4（OSS backend + 方向无关 lazy 迁移作业 + 管理员按钮）等 OSS 到来或需求突增时做；`storage_backend` 列 P2.1 已就位，届时是「加实现」不是「改 schema」。
