# 日历模块化重构方案

## 1. 背景与目标

当前 `frontend/src/views/Calendar/index.vue` 约 2540 行（模板约 430 行、script 约 1670 行、style 约 440 行），同时承担月视图网格、周视图时间轴、拖拽/缩放指针状态机、布局分行算法、颜色/展示工具、事件 CRUD、本地添加/编辑事件弹窗和三处独立的右键菜单。

它已经不是一个普通页面组件，而是一个自成一体的日历子系统，只是还没有被识别成模块。继续在一个文件里维护会让"周视图改坏了会不会牵连月视图""这条颜色规则改了会不会漏改另一处判断"这类问题越来越难回答。

目标：

- 建立 `CalendarItem` 作为日历世界里事件和项目时间线的统一数据形状，业务规则（能否拖拽/缩放、展示色）集中到 `domain/`，不再让组件里出现 `if (type === 'project')` 式的散落判断。
- 把布局算法（月/周分行）拆成纯函数，输入输出明确、可直接单测。
- 把月视图、周视图、拖拽/缩放指针状态机拆成独立模块，`index.vue` 收缩为数据获取 + composable 组合 + 组件组装的协调层。
- 三处独立右键菜单（月格/周列/全天行）合并成一个通用 `ContextMenu.vue`，按 `CalendarContext`（类型 + 目标对象）出菜单项。
- 本地手写的添加/编辑事件弹窗，统一到跨页面共享的 `EventEditModal.vue` + `stores/eventModal.ts`，消掉两套并行实现，并修正组件归属。
- 保持现有拖拽手感、吸附、resize 判定和月/周视图行为不变；拖拽状态机第一阶段只做原样搬家，不顺手优化。
- 分阶段迁移，任何阶段都能独立验证和回滚。

## 2. 当前结构与问题

```text
frontend/src/views/Calendar/
├── index.vue                   # 约 2540 行：数据、状态、布局算法、拖拽状态机、CRUD、
│                                #   弹窗、三处右键菜单全部混在一起
└── components/
    ├── EventEditFields.vue      # 约 89 行：事件表单字段（已抽出）
    └── EventEditModal.vue       # 约 108 行：现有事件弹窗；阶段 3 迁移到跨页面
                                 #   共享的 events 组件目录
```

当前最主要的问题不是行数，而是三类东西没有明确边界：

- **数据形状没有统一**：`CalItem` 接口本身承认"同一数组里既有 `isProject` 也有 `isUserEvent` 的对象"，靠调用方各自判断字段是否存在，规则（能不能拖、显示什么颜色）散落在月视图、周视图、拖拽状态机三处各自的 `if` 分支里。
- **布局算法和组件状态耦合**：`weekBars`/`dayLayout`/`timedLayoutFor` 这类分行算法读写的是组件内的 `ref`/`reactive`，理论上是纯函数却没有纯函数的可测性。
- **拖拽状态机横跨月/周两个视图**：`startDrag`/`_evDragMove`/`onEvResize`/`_wvDrag` 等十几个函数共用同一套指针状态，是全文件里改动风险最高、最容易顺手改坏手感的部分。

## 3. 目标架构

```text
frontend/src/views/Calendar/
├── domain/
│   ├── calendarTypes.ts        # CalendarItem 统一类型（取代散落的 CalItem 用法）
│   ├── calendarNormalizer.ts   # normalizeEvent/normalizeProjectTimeline：分别归一化事件和项目时间线
│   └── calendarRules.ts        # canDrag/canResize/getDisplayColor/typeLabel 等规则集中收口
│
├── components/
│   ├── EventEditFields.vue     # 已有，不动
│   ├── EventEditModal.vue      # 阶段 3 前暂留现处，迁移时归入跨页面共享的 events 组件
│   ├── CalendarToolbar.vue     # 新增：导航按钮 + 月/周视图切换 + 当前月年标签
│   ├── YearMonthPicker.vue     # 新增：年月快速选择弹层
│   ├── CalendarSidebar.vue     # 新增：选中日事件列表 + 近期节点
│   ├── CalendarMorePopup.vue   # 新增：月视图"+K 更多"合并弹窗（项目+事件）
│   ├── ContextMenu.vue         # 新增：合并月格/周列/全天行三处右键菜单，
│   │                            #   接收 CalendarContext（type + target）
│   ├── MonthGrid.vue           # 新增（阶段 5）：月视图网格 + chip 渲染 + 拖框选
│   └── WeekTimeline.vue        # 新增（阶段 5）：周视图日期表头 + 全天行 + 可滚动时间网格；
│                                #   如果拆完仍偏大，再进一步切 WeekAllDayRow/WeekHourGrid
│
└── index.vue                   # 拆完收缩为协调层：拉数据 → 组合 composable →
                                 #   provide 上下文 → 组装组件
```

```text
frontend/src/views/Calendar/utils/
├── calendarColors.ts    # extractAccent/capBg/hexAlpha/darkenHex（纯色值计算，不含规则判断）
└── calendarLayout.ts    # weekBars/weekBarsCached/weekBarsCapped/dayLayout/timedLayoutFor/maxSlots
                          #   纯函数：input CalendarItem[] → output 带位置信息的条目

frontend/src/views/Calendar/composables/
├── useHolidays.ts           # 已有，不动
├── useEventEditForm.ts      # 已有，不动；阶段 3 起被 Calendar/index.vue 更彻底地复用
├── useCalendarNav.ts        # 新增：prev/next/goToday/setView/selectYearMonth/togglePicker，
│                             #   视图/月年/activeRange/todayIso 等导航态
├── useCalendarUpcoming.ts   # 新增：buildUpcomingList，读 domain/calendarNormalizer
└── useCalendarDrag.ts       # 新增（阶段 5A）：startDrag/startEventDrag/startBarDrag/
                              #   startBarResize/_evDragMove/_evDragUp/onEvResize/_wvDrag/_wvUp
                              #   等——月/周视图共用的指针状态机，第一遍只搬家不重构
```

依赖方向单向收口，不允许反向 import：

```text
components → composables → domain/utils
```

`domain/`、`utils/` 不 import 组件或 composable；`composables/` 不 import 组件。

组件之间默认使用 `props`/`emits` 传递数据和动作。只有跨层级、稳定且明确属于 Calendar subsystem 的能力才允许通过 `provide/inject` 传递；不得建立包含 items、导航、拖拽、弹窗和侧栏等全部状态的“大 CalendarContext” service locator。

Calendar 专用的 domain、utils、composables 默认留在 `views/Calendar/` 内；只有出现第二个稳定消费者后，才提升到全局 `src/utils/` 或 `src/composables/`。已有的 `useHolidays`、`useEventEditForm` 是否继续留在全局，按现有真实消费者决定。

`EventEditFields.vue` 和 `EventEditModal.vue` 不应长期属于 Calendar 页面：由于 `DefaultLayout.vue` 和 Calendar 都会使用它们，阶段 3 应将其归入跨页面共享的 events 组件目录，避免形成 `layout → views/Calendar` 的反向依赖。实际目录以现有共享组件结构为准，不为了改名扩大迁移范围。

## 4. 统一数据模型

`CalendarItem` 不直接做成一个所有字段都可选的扁平大对象，而是使用 `type` 判别的联合，保留事件和项目时间线各自的字段边界：

```ts
interface CalendarItemBase {
  id: string | number
  title: string
  start: string
  end?: string
  allDay: boolean
  color: string
}

interface EventCalendarItem extends CalendarItemBase {
  type: 'event'
  description?: string
}

interface ProjectCalendarItem extends CalendarItemBase {
  type: 'project'
  status: string
  currentStage?: string
  progress?: number
}

type CalendarItem = EventCalendarItem | ProjectCalendarItem
```

`color` 如果是用户保存的事件配置，可以作为原始配置字段保留；它不表示最终渲染色。`canDrag(item)`、`canResize(item)`、`getDisplayColor(item)` 是规则的唯一真相，不能再同时保留 `item.draggable`、`item.resizable` 作为并行判断来源。

`calendarNormalizer.ts` 不使用一个同时处理所有来源的 `normalizeEvent`：应拆成 `normalizeEvent(event): EventCalendarItem` 和 `normalizeProjectTimeline(project): ProjectCalendarItem`。如果调用方需要统一入口，再由 `normalizeCalendarItems()` 组合两者。

具体字段以现有 `CalItem`、后端响应和模板实际访问为准，不要求一次性补齐所有业务字段。新增字段应放入对应的判别分支，避免重新形成“大而全”的可选属性集合。

`domain/calendarNormalizer.ts` 分别把后端 `EventResponse` 和项目时间线数据转成对应的 `EventCalendarItem` / `ProjectCalendarItem`；`domain/calendarRules.ts` 提供 `canDrag(item)`/`canResize(item)`/`getDisplayColor(item)` 等函数，月视图、周视图、拖拽状态机、右键菜单统一调用这几个函数判断，不在各自内部重复写 `item.type === 'project'`。

`utils/calendarLayout.ts` 的分行算法只认 `CalendarItem[]`，不认组件的 `ref`/`reactive`，也不感知 Vue：

```text
input:  CalendarItem[]
output: 带 colStart/colEnd/row 等位置信息的条目
```

## 5. 执行计划

> 参照本仓库拖拽系统重构（`docs/refactor/_archive/拖拽系统模块化拆分方案.md`，已归档，方法论仍可参考）和文件浏览系统重构的先例：分阶段、风险递增，每阶段单独可验证、可回滚。**阶段 5（拖拽状态机 + MonthGrid/WeekTimeline）第一遍只做原样搬家，不得顺手改动拖拽手感、吸附、resize 判定或落点逻辑**——这是全文件里唯一"代码质量可能不好、但用户已经习惯其行为"的部分。

### 阶段 0：基线冻结

- [ ] 记录 `Calendar/index.vue` 当前行数、typecheck 状态。
- [ ] 记录月视图、周视图当前的拖拽、resize、吸附、右键菜单行为作为验收基线。
- [ ] 确认不改变事件/项目 API、数据模型和权限边界。
- [ ] 建立布局回归样本：跨天事件、全天事件、重叠事件、同日多事件、无结束时间事件和项目时间线。

### 阶段 1：domain 层 + 纯函数工具

- [ ] 新增 `domain/calendarTypes.ts`，定义 `CalendarItem`。
- [ ] 新增 `domain/calendarNormalizer.ts`，分别收纳 `normalizeEvent` 和 `normalizeProjectTimeline`；需要统一入口时再增加 `normalizeCalendarItems`。
- [ ] 新增 `domain/calendarRules.ts`，收纳 `canDrag`/`canResize`/`getDisplayColor`/`typeLabel`。
- [ ] 新增 `views/Calendar/utils/calendarColors.ts`（`extractAccent`/`capBg`/`hexAlpha`/`darkenHex`）。
- [ ] 新增 `views/Calendar/utils/calendarLayout.ts`（`weekBars`/`weekBarsCached`/`weekBarsCapped`/`dayLayout`/`timedLayoutFor`/`maxSlots`），补纯函数单测锁住行为。
- [ ] `Calendar/index.vue` 改为调用这些模块，暂不改动其余结构。
- [ ] 每个布局函数先用旧实现的输入/输出建立回归测试，再替换调用点；测试通过前不调整排序、分行、截断或吸附相关行为。
- [ ] 重点验证跨天归属、全天事件、重叠排序、最大行数和缺少结束时间等边界，不能只用普通单事件样本验收。

### 阶段 2：低耦合展示组件

- [ ] 抽出 `CalendarToolbar.vue`。
- [ ] 抽出 `YearMonthPicker.vue`。
- [ ] 抽出 `CalendarSidebar.vue`，读 `useCalendarUpcoming`。
- [ ] 抽出 `CalendarMorePopup.vue`。
- [ ] 新增 `composables/useCalendarNav.ts`、`composables/useCalendarUpcoming.ts`。

### 阶段 3：弹窗统一

- [ ] `Calendar/index.vue` 的本地 `showEditForm`/`editingEvent`/添加事件弹窗改为复用全局 `EventEditModal.vue` + `stores/eventModal.ts`。
- [ ] 确认全局弹窗语义（打开/关闭/保存/删除）覆盖日历页原有的行为；有出入则先补齐全局弹窗，不在日历页另开分支。
- [ ] 删除日历页里因此变成重复的本地弹窗模板和状态。

### 阶段 4：右键菜单合并

- [ ] 定义 `CalendarContext` 判别联合，禁止使用无关字段都可选的 `type + target` 宽对象：

  ```ts
  type CalendarContext =
    | { type: 'month-cell'; date: string }
    | { type: 'week-column'; date: string }
    | { type: 'allday'; date: string }
    | { type: 'event'; item: CalendarItem }
  ```

- [ ] 新增 `ContextMenu.vue`，按 `CalendarContext` 出菜单项。
- [ ] 月格、周列、全天行三处右键菜单调用改为传入 `CalendarContext`，删除原来三份独立菜单模板。

### 阶段 5：拖拽状态机 + 月/周视图（风险最高，最后做）

- [ ] **阶段 5A：只搬迁 `useCalendarDrag.ts`。** `Calendar/index.vue` 的 DOM 和模板完全不拆，单独验收月/周拖拽、resize、吸附和 pointer 生命周期。
- [ ] **阶段 5B：只抽出 `MonthGrid.vue`。** 拖拽状态机保持上一阶段实现不变，单独验收月视图网格、chip、拖框选和月格菜单。
- [ ] **阶段 5C：只抽出 `WeekTimeline.vue`。** 拖拽状态机和月视图保持不变，单独验收周视图日期表头、全天行、时间网格、拖拽和 resize。
- [ ] `WeekTimeline.vue` 如果仍然过大，再另开后续提交拆 `WeekAllDayRow.vue`/`WeekHourGrid.vue`，不在 5C 中顺手扩大范围。
- [ ] `Calendar/index.vue` 收缩为协调层：数据获取 + composable 组合 + `provide` 上下文 + 组件组装。
- [ ] 每个子阶段独立提交和手动验收，确认手感未变，不将 composable 搬迁、DOM 拆分和样式整理混在同一提交。

## 6. 风险与控制

### 数据模型风险

- `CalendarItem` 采用 `event`/`project` 判别联合；归一化必须覆盖现有 `CalItem` 承载的所有字段场景（月视图 chip、周视图条目、侧栏、"更多"弹窗、拖拽 item），确认没有静默丢字段。
- 不允许通过大量可选字段重新制造一个扁平大对象；新增业务字段应归入对应的 `type` 分支。
- 规则收口到 `domain/calendarRules.ts` 后，必须删除组件和 composable 里原有的重复判断，不能两处并存导致以后改一处漏一处。

### 布局算法风险

- `calendarLayout.ts` 必须保持纯函数，不得读取 Vue 的 `ref`/`reactive` 或组件 DOM。
- 阶段 1 的目标是等价迁移，不是重新设计布局；跨天、重叠、全天和最大行数规则必须由回归测试固定。
- 回归测试应覆盖月视图和周视图共同使用的边界，避免只验证其中一个视图导致另一侧行为漂移。

### 拖拽风险

- 月视图和周视图共用同一套指针状态机，阶段 5 搬家时不得拆散状态归属，也不得为了"看起来更干净"合并/拆分状态字段。
- resize 边缘判定（`_evEdge`）、拖拽阈值、吸附（`_snapMin`）都是隐含产品需求，迁移前后必须逐条对照。

### 依赖方向风险

- `domain/`、`utils/` 不得 import 组件或 composable；一旦出现反向依赖，视为架构违规，需要在评审时拦下。

### DOM/CSS 拓扑风险

- 阶段 5 的第一轮组件抽取不得改变原有 DOM 拓扑；不得为了组件语义增加无必要 wrapper。
- 原有 class、节点层级、`position` containing block、scroll container、`overflow` clipping、stacking context、`z-index` 和 pointer hit-test 必须保持一致。
- `getBoundingClientRect()`、pointer capture、`querySelector` 范围和弹层定位参照物都要在迁移前后逐项对照。
- 父组件的 scoped CSS 不会自动作用于新组件内部 DOM；相关样式随组件一起迁移，第一轮不顺手整理 CSS。
- 5A 只搬状态机，5B/5C 才搬 DOM；每次只改变一个边界，出现拖拽或定位回归时可以明确归因。

### 弹窗统一风险

- 共享 `EventEditModal.vue` 接入日历页前需要确认参数/事件契约是否够用，不够则先扩展共享弹窗，不在日历页另建分支绕过去。

## 7. 最终验收 Todo

### 月视图

- [ ] 月格拖框选创建事件正常。
- [ ] chip 拖拽移动到其他日期正常。
- [ ] "+K 更多"弹窗内容和交互正常。
- [ ] 月格右键菜单（创建事件/创建项目）正常。

### 周视图

- [ ] 全天行项目跨天条、无时间活动展示正常。
- [ ] 时间网格拖拽新建、拖动、resize 事件正常，吸附手感不变。
- [ ] 周列/全天行右键菜单正常。

### 弹窗与侧栏

- [ ] 添加/编辑事件弹窗（改走全局 `EventEditModal.vue` 后）功能不回归。
- [ ] 侧栏选中日事件列表、近期节点展示和跳转正常。
- [ ] 提醒渠道测试功能正常。

### 工程验证

- [ ] `npm run typecheck` 通过。
- [ ] `views/Calendar/utils/calendarLayout.ts` 补齐单元测试。
- [ ] 布局纯函数验证输入不被修改：执行前保存输入快照，执行后确认输入保持相等。
- [ ] 布局纯函数验证确定性：相同输入重复计算得到相同布局结果。
- [ ] 拖拽状态机验证日历外 `pointerup`、切月过程和组件卸载时，监听器与 pointer capture 都能清理。
- [ ] `Calendar/index.vue` 不再包含具体布局算法、拖拽物理和右键菜单模板。
- [ ] 更新 CHANGELOG 和 devlog。
