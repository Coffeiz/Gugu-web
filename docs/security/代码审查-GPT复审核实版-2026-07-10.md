# Gugu-web 代码审查报告(GPT 复审 · 主审核实版)

> 日期:2026-07-10 · 范围:前端安全 + 工程质量(marked/v-html、测试地基、TS 严格度、时区、模块体量、异常处理)。
> 来源:外部模型(GPT)做了一次全项目代码审查 → 主审(Claude)对**每一条**逐项 grep + read 核实,标注核实状态。
> 核实状态:✅ 实测确认 / ⚠️ 属实但需补充 / ❌ 误报。
> 修复状态:✅ 已修 / 🔧 起步(棘轮已建、增量推进) / ⬜ 待办。
> 说明:本文初版(07-10)只做**审查 + 修复方案**;此后陆续实修。**当前进度(2026-07-11)**:P0 / P1-a / P1-c / P3 已完成 ✅;P1-b 独立部分完成(棘轮 83 源文件,含 Admin/Agent);P2-a 已起步(抽 projectStages 纯函数 + 测试,见 §6 执行计划);P2-b 待办。修复进度记在下表「修复」列,各节内标注落地情况。

---

## 0. 一句话结论

**这份外部审查可信、可照单收**——7 条结论逐项核实全部属实,连计数级细节都基本对得上(异常 287 处分毫不差,模块行数全中)。

唯一真正紧急的是 **P0:通用 Markdown 链路把未消毒的 HTML 直出到 `v-html`**(marked v18 无 sanitizer + 全站无 DOMPurify)。本次核实到的**已证实影响面是 self-XSS**;跨用户/跨权限升级路径(后台查看用户内容、群聊互渲染、通知插用户串)**未在本轮证实**,但漏洞类确凿、修法明确,**self-XSS 本身也必须堵**。其余为工程健壮性欠账(无前端测试、TS 未严格、naive UTC、巨型模块、异常过宽),不是即时漏洞但会持续抬高回归与排障成本。

**修复顺序**:P0 XSS → 前端测试地基(先覆盖思维记录/日历/项目编辑) → 时区统一方案 → 按领域逻辑拆大模块。

---

## 1. 结论总览

| # | 结论 | 严重度 | 核实 | 修复 | 关键证据 |
|---|------|--------|------|------|----------|
| P0 | Markdown 未消毒直出 `v-html`(XSS) | 高 | ✅+⚠️ | ✅ 已修 | 全站无 DOMPurify;marked v18 无 sanitizer;`MarkdownView.vue:2` / `TextViewer.vue:40` 裸 v-html → 已上 DOMPurify 全 sink 消毒 |
| P1-a | 前端无自动化回归层 | 中 | ✅ | ✅ 已建 | `vitest` 已接入,4 文件 57 测试(markdown 消毒 / dateAttribution / projectProgress / useMindEditor) |
| P1-b | TypeScript 仍"尽力检查" | 中 | ✅ | 🔧 独立部分完成 | 建 `tsconfig.strict.json` **文件级棘轮** + `typecheck:strict` 门禁;**83 源文件 strict-clean**（底座 + 全部中小组件 + Admin/Agent）;余 4 个巨型视图（Calendar/Files/GuguChat/ProjectModal）归 P2-a 拆分时一起类型化 |
| P1-c | 时间语义混用 naive UTC | 中 | ✅ | ✅ 已修 | 后端 `utcnow()` 55 处 → 统一 `now_utc()` + `UtcDateTime`/timestamptz + 用户时区归属(见时区迁移方案) |
| P2-a | 核心模块过于集中 | 低 | ✅ | 🔧 起步 | 执行计划见 §6；第一刀已起步：抽 `utils/projectStages.ts` 纯领域函数 + 17 Vitest（未迁调用点） |
| P2-b | 异常处理过宽 | 低 | ✅ | ⬜ 待办 | `except Exception`/裸 except 287 处；方案见 §7 |
| P3 | 少量未完成入口 | 提示 | ✅ | ✅ 已修 | `CalendarPanel.vue` `selectDay` 已实现：点某天→跳日历定位到该日（pendingCalendarDate 信号，复用 GlobalSearch 同款路径） |

**正面(核实属实)**:后端全量 pytest 179 passed;归属守卫(`get_owned`)与不可逆操作确认门在位、静态检查通过;多用户访问边界是项目里相对扎实的一层——**不建议为重构去动它**。

---

## 2. P0 — Markdown 未消毒直出 `v-html`(XSS)✅ 实测确认 · 🛠️ 已修复(2026-07-10)

> **修复记录(2026-07-10)**:按下方方案落地并实测通过。
> - 引 `dompurify@^3.4.11`(唯一新增依赖);`markdown.ts` 建全站唯一安全出口 `sanitizeHtml`(marked → DOMPurify),`renderMarkdown` 经它;link renderer 加**协议白名单**(`http(s)/mailto/tel/#/相对`)+ `href`/`title` **属性转义**。
> - 所有 v-html sink 接入:`MarkdownView`(text **与** html 预渲染 prop 都消毒,覆盖通知 + GuguChat 聊天)、`TextViewer.mdHtml`、`Privacy`;`Admin/Notifications` 走 `renderMarkdown` 自动覆盖;Mind `NoteCard`(本就转义)未动。
> - 实测(jsdom + DOMPurify):`<img onerror>` / `<script>` / `<svg onload>` / `javascript:` 链接**全部中和**;hljs 高亮 span、代码块复制按钮+内联 SVG、`target=_blank rel=noopener` 链接**渲染无损**。vue-tsc 通过。
> - **回归已固化**(2026-07-11):Vitest 落地(见 §3),`src/utils/markdown.test.ts` 13 条断言钉死"危险载荷中和 + 正常渲染无损 + title 属性逃逸不产生真实元素"。跨用户升级路径(§9 待证实项)仍建议核实。

### 现状(核实)

- **全站无 HTML 消毒**:`dompurify` / `sanitize-html` 在依赖与源码里都搜不到。
- **marked v18 默认不消毒**:`sanitize` 选项自 marked v5 起已移除。本地事实:`<img src=x onerror=alert(1)>` 原样保留。
- **裸 `v-html` sink**:
  - [`MarkdownView.vue:2`](../../frontend/src/components/common/MarkdownView.vue:2) — `v-html="rendered"`,`rendered = renderMarkdown(text)`(裸 marked)或预渲染 `html`;喂入方:`AppSidebar.vue:131`(通知)、`NotificationBubble.vue:19`(通知)、`GuguChat.vue:203`(咕咕回复 `msg.html`/流式)。
  - [`TextViewer.vue:40`](../../frontend/src/components/common/viewers/TextViewer.vue:40) — `v-html="mdHtml"`,其独立 `new Marked()` **只对代码块 `escHtml`**,正文/内联 HTML 直出;喂入方:用户上传的 `.md/.txt` 文件正文。
  - `Privacy.vue:16`(静态隐私政策,低危)、`Admin/Notifications/index.vue:61`(管理员预览自己草稿,低危)。
- **`markdown.ts` link renderer 有额外注入面(GPT 未点出)**:[`markdown.ts`](../../frontend/src/utils/markdown.ts) 自定义 link 把 `href` **与** `title` 直接字符串插值、都不转义 —— 不仅 `javascript:` 协议能过,`title` 里 `"><img onerror=…>` 还能**属性逃逸**。
- **Mind 便签预览是安全的(GPT 说法属实)**:`NoteCard.vue:36` 走 `mdToPreviewHtml`,该函数用 `_ESC`(`& < > "` → 实体)**先转义再拼**,不受影响。

### 严重度校准(主审补充)

技术上确凿。本轮追到的**已证实**利用面是 **self-XSS**:用户把 `<img onerror>` 存进项目名/便签/文件正文,诱导咕咕回显(聊天 MarkdownView)或自己打开该 `.md`(TextViewer)→ 在**自己**浏览器执行。**跨用户/跨权限升级**(后台管理员用同一 marked 渲染用户内容而被弹、IM 群聊互相渲染、通知里嵌用户可控串)**本轮未证实**,是待查项而非已确认。**结论:优先级维持"先修",但影响面标注为"已证实 self-XSS + 跨用户待证实",不夸大为已确认的跨租户 Critical。**

### 修复方案

1. **引依赖**:`npm i dompurify` + `npm i -D @types/dompurify`(纯前端、无外部请求,符合仓库无新增外链原则)。
2. **收敛成单一安全出口**:在 [`markdown.ts`](../../frontend/src/utils/markdown.ts) 内把 `renderMarkdown` 改为 `marked.parse(...)` → `DOMPurify.sanitize(html, CONFIG)`,**所有** v-html 都必须经它。
   - `MarkdownView` 的 **`html` 预渲染 prop 也要 sanitize**——它来自服务端/流式,同样不可信,不能因为"已是 HTML"就跳过。
   - `TextViewer` 的 `mdHtml`、`Privacy` 的 `html`、`Admin/Notifications` 预览、`GuguChat` 的 `renderMdStream`/`msg.html` 全部改走同一 sanitizer。
   - Mind 的 `mdToPreviewHtml` 已安全,统一后可让它也复用同一出口(去掉手写 `_ESC` 的维护面),非必须。
3. **DOMPurify 配置**(建议):默认即禁 `on*` 事件属性与 `<script>`;额外 `FORBID_TAGS: ['style','iframe','object','embed','form']`;`ALLOWED_URI_REGEXP` 限 `href`/`src` 协议为 `http/https/mailto/tel/#/相对路径`;`ADD_ATTR:['target']` 保留新标签打开;确认 `class` 在白名单内(hljs 代码高亮产物依赖 `class` span,别被吃掉)。
4. **修 link renderer**:要么删掉自定义 renderer、让 DOMPurify 统一处理后用 `afterSanitizeAttributes` hook 给 `<a>` 补 `target=_blank rel=noopener`;要么保留但对 `href` 做协议白名单校验、`title` 走属性转义。**二选一,别再裸插值**。
5. **图片外链决策**:`<img>` 允许外链 src 会带来 SSRF 追踪像素/隐私泄露面,结合产品需要决定是否 `FORBID_TAGS` 含 `img` 或限制 `src` 协议(至少禁 `javascript:`/`data:` 非图片)。
6. **回归验收**(依赖 P1-a 的 Vitest 落地):断言 `<img src=x onerror=alert(1)>`、`<script>`、`[x](javascript:alert(1))`、`title="\"><img onerror=…>"` 经安全出口后**均被中和**;并确认正常 md(加粗/表格/代码高亮/链接新标签打开)不被误伤。

---

## 3. P1-a — 前端无自动化回归层 ✅ 实测确认 · 🛠️ 地基已落地(2026-07-11)

> **进度(2026-07-11)**:Vitest 地基已搭(`vitest` + `jsdom` devDep、`vite.config.js` 加 `test` 块 jsdom 环境、`package.json` 加 `test`/`test:run` 脚本)。纯逻辑测试已覆盖高变化区,**36 条全过**:
> - `src/utils/markdown.test.ts`(13)—— P0 XSS 回归(危险载荷中和 + 正常渲染无损 + title 逃逸不产生真实元素)
> - `src/composables/useMindEditor.test.ts`(17)—— Mind Markdown⇄doc 往返无损 + 幂等 + 标题 clamp + 引用保真(含嵌在待办里)+ 待办勾选 + 预览转义
> - `src/utils/projectProgress.test.ts`(6)—— 全站统一进度口径:待办占比主口径 + 阶段位置兜底 + 边界
> **待办**:日历「日期归属/今天·本周」目前散在组件内、无纯函数出口,值得抽出后再测;Playwright 覆盖登录后思维记录/日历/项目编辑三条 e2e(需可运行的联调环境,另行安排)。

**现状**:`package.json` 只有 dev/build/typecheck/gen:types,无 test 脚本;`src` 下无任何 `.test/.spec`。近期高交互区(日期条、卡片层叠、弹层切换动画)只能人工回归,视觉/状态机回归易反复。

**修复方案**:
1. 先补 **Vitest** 纯逻辑测试(零 DOM 成本,ROI 最高):Markdown 安全出口(见 P0 §6)、Mind 的 Markdown⇄doc 往返、时间/日期归属换算、留存/统计口径的纯函数。加 `"test": "vitest"` 脚本。
2. 再上 **Playwright** 覆盖登录后**最高变化的三条路径**:思维记录、日历、项目编辑;先各一条 happy-path 冒烟,挡住"整页崩/关键交互失效"。
3. 顺序:Vitest 地基 → 三条 e2e 冒烟 → 逐步补。**不追求覆盖率数字,先挡住高频回归区。**

---

## 4. P1-b — TypeScript 仍"尽力检查"✅ 实测确认

**现状**:[`tsconfig.json`](../../frontend/tsconfig.json) `checkJs:false` / `strict:false` / `noImplicitAny:false`(渐进迁移的有意起点,注释已写明);`: any`/`as any`/`<any>` 共 **62 处**(GPT 说~51,方向对)。非即时 bug,但会让 API 字段漂移、复杂交互状态错误更晚暴露。

**修复方案**:不全仓一刀切。**按模块开局部严格**——优先 Mind / Calendar / Projects(变化最快、状态最复杂)。手段:这些目录下新代码禁 `any`;给 API 响应类型收紧(配合 `gen:types` 的 openapi 类型);逐步把 `as any` 换成真实类型或 `unknown`+收窄。跑通一个模块再推下一个。

**🔧 已落地(2026-07-11)——文件级棘轮机制**:

- **机制**:新增 [`tsconfig.strict.json`](../../frontend/tsconfig.strict.json)(`extends` 主档、`strict/noImplicitAny` 全开)+ `npm run typecheck:strict` 脚本。主 `tsconfig.json` 保持全仓渐进(`strict:false`),`build`/`typecheck` 常绿不受影响;严格档只作用于 `include` 白名单,**清干净一个 → 加进白名单 → `typecheck:strict` 必须常绿**(提交前门禁)。
- **粒度=文件,不是"模块"**:实测发现 strict 跨整个 program 报错,而 Vue 组件的 import 闭包会扇出到共享基建(stores / composables / common 组件)——把整个 Projects 模块入档一次牵出 **838 个存量错、涉 30 文件**(半个 app)。故棘轮从**叶子**(util / type,闭包小且自洽)起步,把 store/composable 类型补齐后再向组件扩张。这也是"不全仓一刀切"的具体落地形态。
- **已入档(strict-clean)——83 源文件**（下方为初期明细，最新清单/进度见「接力顺序」条与 §6 执行计划）:
  - `src/types/**`(含新建领域模型 [`types/project.ts`](../../frontend/src/types/project.ts),绑定 OpenAPI `ProjectResponse` + 收紧 status/stages)、`src/utils/**`、`src/services/**`(api / cache);
  - stores:`projects.ts`(105→0,`ref<Project[]>` 消 ~80)、`ui.ts`(通知/信号 store)、`live.ts`(SSE 实时 store);composable:`useOnboarding.ts`。
- **收窄的显式 any**:Projects 组件 `PropType<any[]>` → `PropType<Project[]>`(DoneColumn / KanbanColumn)、ArchivedProjectsModal `Map<string, any[]>` → `Project[]`。
- **api 边界收紧模式**:wire 类型(生成的 `ProjectResponse`,`status:string` / `stages` 未结构化)在 store 的 fetch/create/update 处一次性 `as unknown as` 收成紧类型,下游全用紧类型——单点、有注释(见 `stores/projects.ts`)。
- **接力顺序**:✅ ① stores 底座(projects/ui/live)+ useOnboarding → ✅ ② 轻量 store/composable 尾批(10 文件、51 错)→ ✅ ③ 文件簇:③-a filesCache/preview/useUploadQueue/useBoxSelection(定义 `FileMeta`=FileResponse+客户端增补交集、`FolderMeta`=FolderResponse;preview 承载聊天附件用 `Partial<FileMeta>`)· ③-b usePhysicsDrag(959 行/67 错→0,`Box`/`ActiveDrag` 类型 + `event: PointerEvent|DragEvent` 联合就地 cast,纯类型标注零逻辑改动)+ useFileDragDrop → ✅ 底座补齐(auth/useLazyThumb 两个漏网基座)+ 🔧 ⑤ 组件层起步:已入档 **20 个 common 组件**（common 层仅剩 GuguChat + MarkdownView）(NavItem/PdfViewer/FeedbackModal/FileInfoPopup/DatePicker/DateSpanPicker/ImageViewer/VideoViewer/AppSidebar/GlobalSearch/GlassBg/UploadConflictDialog/AvatarCropper/SegBar/NotificationBubble/TextViewer/FilePreviewModal/FloatPreviewWindow——闭包只依赖已净的 stores/composables + BaseModal)。多是模板 `ref<HTMLElement\|null>`、事件参数、`catch(e:unknown)`、给搜索结果/通知等定小领域类型;顺带把 ui.ts 的 `pendingFileTarget/pendingCalendarEvent/pendingChatMessageId` 补了类型。→ 底座 + common（除 GuguChat）+ 全部 Admin 页（除 Agent）+ Schedules + auth 页均已入档；剩 4 个巨型视图（Calendar/Files/GuguChat/ProjectModal，Agent 已类型化）——归 P2-a 拆分时一起类型化（见 §6 执行计划），不单独纯堆类型。每步跑通 `typecheck:strict` 再推下一步。**当前棘轮共 83 源文件——底座 + 全部中小组件 + Admin/Agent 全 strict-clean。** ④(Calendar `CalendarEvent`、Mind `NoteEditor` TipTap any)与组件入档并行,Mind 相关待并发的思维面板重构稳定后再碰。

---

## 5. P1-c — 时间语义混用 naive UTC ✅ 实测确认 · 🛠️ 已修复(2026-07-11)

> **修复记录(2026-07-11)**:按下方方案分阶段落地完成。统一时钟 `now_utc()`（aware UTC）+ 静态守卫拦裸 `utcnow()`；`DateTime` 列迁移为 `UtcDateTime`/`timestamptz`；「日期归属」按用户时区换算（Calendar/`captured_at`/提醒/留存 + 前端 dateAttribution + admin 视口时区）。详见 [时区与统一时钟迁移方案](../backend/时区与时钟迁移方案.md)。

**现状**:后端 `datetime.utcnow()` **55 处**(GPT~54),业务路由直调(如 [`mind.py:74`](../../backend/app/api/v1/mind.py:74) `captured_at = body.captured_at or datetime.utcnow()`)。运行环境 **Python 3.14**(GPT 写的 3.12 有误,但弃用告警点成立——测试输出里持续刷 `utcnow() is deprecated`)。naive UTC 会持续制造"今天/未来日期"边界问题。

**修复方案**:➡️ 详见 **[时区与统一时钟迁移方案](../backend/时区与时钟迁移方案.md)**(2026-07-11,分阶段可回滚)。策略摘要:
1. 立**统一时钟**:`now_utc()`(aware UTC),业务代码一律走它、不再直调 `utcnow()`(加静态守卫拦裸调用)。
2. 定**存储/展示分层**:存储 aware UTC(列 `DateTime` → `DateTime(timezone=True)`,现值本就是 UTC、迁移贴 tzinfo 即可);"展示"与"**日期归属**"按用户时区换算——这是"今天/未来日期"边界 bug 的根因。
3. 先治**日期归属敏感点**(日历、`captured_at`、提醒、留存窗口),再清扫其余 `utcnow()`;新代码即刻走统一时钟先止血。

> **迁移前置(核实新增)**:后端 **42 个 DateTime 列全 naive**、且 `User` 上**根本没有时区字段**——"展示按用户时区"在加 `User.timezone` 之前**做不了**。故迁移必须从 Phase 0(用户时区字段)起步,不能直接改 `utcnow()`。另:aware/naive **混比会 TypeError**,统一时钟不能中间态零散引入,须按方案分阶段。

---

## 6. P2-a — 核心模块过于集中 ✅ 实测确认 · 🔧 起步(2026-07-11)

**现状**:ProjectModal 3036 / Files 2709 / Calendar 2569 / Chat 2254 行;后端 files.py 1375 + files API 1275。单文件同时扛状态、请求、DOM 事件、动画、业务规则,改一个交互常牵动多条隐式耦合。

**修复方案**:**优先拆"纯状态/领域逻辑",不是只拆 UI**。第一刀切**项目阶段 + 待办 schema**——它现在同时散落在前端 store、新建项目弹窗、Agent 工具三处,抽成单一领域模块/类型源(前后端共享定义),消除"三处各改一遍"。第二刀切**文件操作**(files.py / files API 的领域规则)。UI 拆分次之,别为拆而拆。

### 执行计划（采纳版，2026-07-11）

> P2-a 与 P1-b 的「巨型视图尾巴」是同一批文件——**在拆分时顺手类型化 = P1-b 顺带完成**，不为堆类型而堆、也不重复劳动。计划记忆见 `gugu-p2-refactor-plan`。

1. **Admin/Agent 纯类型化**（不属拆分，纯 P1-b）：不为行数硬拆，先收紧 API 响应/表单状态/事件参数的 any 建局部严格范例。→ ✅ **已完成**（224→0，棘轮 83 文件）。
2. **项目阶段/待办：先抽纯领域函数 `utils/projectStages.ts`（不是一上来就 composable）**：`autoCompleteTodos`/`restoreTodos`（完成快照 + 撤销还原）、`toggleTodoDone`、`nextStatus`、`stageProgressByIndex`、`normalizeStages`——有输入输出、易单测、可被 store / NewProjectModal / ProjectModal / Agent 工具共用；响应式编排真重复了再包薄 composable。→ ✅ **纯函数 + 17 Vitest 已落地**（未迁调用点、零行为改动）。
3. **迁移调用点：Store → 新建项目弹窗 → ProjectModal → Agent 工具**，每迁一处走标准 UI 路径验收（阶段推进/勾待办/自动完成/撤销/刷新落库）。ProjectModal 迁移时顺带类型化（184 类型错随之下降）。→ ⬜ **待做**。
4. **文件域第二刀，前后端不混一次大改**：前端先抽选择/拖拽/移动/上传状态 + API client 类型；后端 files.py 授权/存储/异步任务单独拆。**红线：不碰坏 `get_owned()` 与删除确认门。** → ⬜ **待做**。
5. **Calendar、GuguChat 最后**：只抽纯计算/独立 UI 子区，不动交互主循环；GuguChat 等它那带（Markdown/流式/尺寸动画，并发在改）稳定再拆。→ ⬜ **待做**。

**每步纪律门槛**：先补受影响领域纯函数测试再重构 · 局部开严格棘轮不强推全仓 · 交互页 typecheck 不算验收（走一遍关键 UI 流程）· 每一刀独立提交，不混「拆组件 + 改行为 + 批量格式化」。

> **人工验收清单**：迁移调用点时逐项走标准 UI 路径 → [docs/product/功能验收清单.md](../product/功能验收清单.md)（A 段=阶段/待办迁移核心，B/C=新建·Agent，D=本轮已做改动回归）。

---

## 7. P2-b — 异常处理过宽 ✅ 实测确认

**现状**:`except Exception`/裸 except **287 处**(与 GPT 的~287 分毫不差),在 Agent、适配器、文件、配置链路尤其密集。多数是合理的外部 I/O 防护,但缺统一约束,易把编程错误(AttributeError/KeyError)误报成普通失败、抬高排障成本。

**修复方案**:定一条**约束**而非逐处重写——

**执行计划（采纳版，2026-07-11）**:先立规则、后分链路收债,**别粗暴规定「所有 except 必须写日志」**(那会制造噪声)。
- **外部 I/O / 适配器 / 后台任务**:必须**分清三类错误**——可预期错误(正常业务分支，静默或轻提示)/ 可重试错误(退避重试 + 计数)/ 未知错误(结构化日志 + 错误类别，不吞编程错误 AttributeError/KeyError)。
- **领域逻辑**:只捕获**预期异常**类型,禁裸 `except Exception`。
- 落地:先把这条约束写进 CONTRIBUTING/agent 文档 → 新代码强制 → 存量**从 Agent 与 IM 适配器开始**分链路收敛 → 加轻量静态守卫抓「裸 except + 未分类」。

---

## 8. P3 — 未完成入口 ✅ 实测确认 · 🛠️ 已修复(2026-07-11)

> **修复记录(2026-07-11)**:选「实现它」。`CalendarPanel.selectDay` 现在点某天（含相邻月灰格）→ 设 `uiStore.pendingCalendarDate = d.iso` → `router.push('/calendar')`，Calendar/index 用 `immediate:true` watch 接住并定位到该日所在月+选中该日，复用 GlobalSearch 日程搜索跳转的成熟路径。顺带修好 CalendarPanel 小日历格 `d.iso` 恒 undefined 的潜在 bug（假日徽章/跳转在相邻月失效）。

**原现状**:[`CalendarPanel.vue`](../../frontend/src/views/Dashboard/components/CalendarPanel.vue) `function selectDay(d) { /* TODO */ }` 空实现——"看起来可点、点了没反应"。

---

## 9. 建议执行顺序

1. **先修 P0 XSS**(引 DOMPurify + 收敛单一安全出口 + 修 link renderer + URL 协议白名单)。
2. **建前端测试地基**(Vitest 纯逻辑先行,含 P0 的 XSS 回归断言),覆盖思维记录 / 日历 / 项目编辑三个高变化区的冒烟。
3. **制定时间/时区迁移方案**(统一时钟 + 存储 aware UTC + 展示按用户时区),先治日期归属敏感点。
4. **按领域逻辑拆大模块**,从"项目阶段+待办 schema"和"文件操作"开始。

> 待证实项(不阻塞修复,但影响 P0 影响面结论):后台管理面板是否用同一 marked 渲染用户内容、IM 群聊的跨用户渲染边界、通知内容是否嵌用户可控串——任一成立则 P0 从 self-XSS 升级为跨用户/跨权限存储型 XSS。
