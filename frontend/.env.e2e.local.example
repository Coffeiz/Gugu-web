# 复制成 .env.e2e.local（已在 .gitignore 里，不会被提交）并填上真实值，
# 然后跑 e2e/run-local.sh。只用于本地/devserver 手动跑 e2e，CI 走的是
# .github/workflows/runtime-integration.yml 里的 seed_e2e_user.py，不用这个文件。

PLAYWRIGHT_USERNAME=
PLAYWRIGHT_PASSWORD=
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5173
