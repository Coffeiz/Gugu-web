# Compose 内置 SearXNG 配置。
# 这是开发/内网默认配置；生产环境请替换 secret_key，并在网络层限制访问来源。
use_default_settings: true

server:
  secret_key: "gugu-compose-dev-change-me"
  limiter: false

# Gugu 的 web_search/image_search 直接请求 JSON；缺少 json 会返回 403。
search:
  formats:
    - html
    - json
